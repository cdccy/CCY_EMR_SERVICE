# CCY_EMR_SERVICE 数据库全量设计报告

- 数据库：`emr_db`
- Schema：`public`
- 生成时间：2026-04-16 10:23:03 +0800
- 元数据来源：数据库实时查询（`information_schema`、`pg_catalog`）
- 范围：49 张基表、6 个视图、23 个触发器（以本次查询快照为准）

## 目录
1. 主数据表
2. 业务数据表
3. 视图
4. 触发器与触发器函数
5. 系统配置表附录

## 1. 主数据表

本模块覆盖医疗主数据及模板/字典主数据，支撑诊疗业务参数化配置。

### doctor

- 表功能：医生主数据表：描述医疗业务中的医生实体，与系统用户账号解耦（来源：数据库注释）
- 表注释：医生主数据表：描述医疗业务中的医生实体，与系统用户账号解耦
- 字段数：10

**字段结构与功能**

| 序号 | 字段名 | 类型 | 可空 | 默认值 | 字段功能说明 |
|---:|---|---|---|---|---|
| 1 | id | bigint | 否 | nextval('doctor_id_seq'::regclass) | 主键ID（来源：数据库注释） |
| 2 | doctor_code | varchar(64) | 否 | （无） | 医生业务编码：全院唯一，用于所有业务表关联（来源：数据库注释） |
| 3 | doctor_name | varchar(128) | 否 | （无） | 医生姓名（来源：数据库注释） |
| 4 | dept_code | varchar(64) | 否 | （无） | 所属科室编码（来源：数据库注释） |
| 5 | title_code | varchar(32) | 是 | （无） | 职称编码：如主任医师/主治医师（来源：数据库注释） |
| 6 | license_no | varchar(64) | 是 | （无） | 执业医师证号（来源：数据库注释） |
| 7 | user_username | varchar(50) | 否 | （无） | 绑定的系统登录账号（sys_user.username）（来源：数据库注释） |
| 8 | enabled | boolean | 是 | true | 是否在岗启用（来源：数据库注释） |
| 9 | create_time | timestamp without time zone | 是 | CURRENT_TIMESTAMP | 创建时间（来源：数据库注释） |
| 10 | update_time | timestamp without time zone | 是 | CURRENT_TIMESTAMP | 更新时间（来源：数据库注释） |

**关键约束与关系**

- [f] fk_doctor_dept：FOREIGN KEY (dept_code) REFERENCES sys_department(dept_code)
- [f] fk_doctor_user：FOREIGN KEY (user_username) REFERENCES sys_user(username)
- [p] doctor_pkey：PRIMARY KEY (id)
- [u] doctor_doctor_code_key：UNIQUE (doctor_code)
- [u] doctor_user_username_key：UNIQUE (user_username)

**索引**

- doctor_doctor_code_key：CREATE UNIQUE INDEX doctor_doctor_code_key ON public.doctor USING btree (doctor_code)
- doctor_pkey：CREATE UNIQUE INDEX doctor_pkey ON public.doctor USING btree (id)
- doctor_user_username_key：CREATE UNIQUE INDEX doctor_user_username_key ON public.doctor USING btree (user_username)
- idx_doctor_dept_code：CREATE INDEX idx_doctor_dept_code ON public.doctor USING btree (dept_code)
- idx_doctor_user_username：CREATE INDEX idx_doctor_user_username ON public.doctor USING btree (user_username)

### doctor_favorite_template

- 表功能：医生个人收藏的病历模板关系表（来源：数据库注释）
- 表注释：医生个人收藏的病历模板关系表
- 字段数：4

**字段结构与功能**

| 序号 | 字段名 | 类型 | 可空 | 默认值 | 字段功能说明 |
|---:|---|---|---|---|---|
| 1 | id | bigint | 否 | nextval('doctor_favorite_template_id_seq'::regclass) | 主键ID（来源：数据库注释） |
| 2 | doctor_code | varchar(64) | 否 | （无） | 医生编码（来源：数据库注释） |
| 3 | template_code | varchar(64) | 否 | （无） | 病历模板编码（来源：数据库注释） |
| 4 | create_time | timestamp without time zone | 是 | CURRENT_TIMESTAMP | 收藏时间（来源：数据库注释） |

**关键约束与关系**

- [f] fk_fav_doctor：FOREIGN KEY (doctor_code) REFERENCES doctor(doctor_code) ON DELETE CASCADE
- [f] fk_fav_template：FOREIGN KEY (template_code) REFERENCES emr_template(template_code) ON DELETE CASCADE
- [p] doctor_favorite_template_pkey：PRIMARY KEY (id)
- [u] uk_doctor_template：UNIQUE (doctor_code, template_code)

**索引**

- doctor_favorite_template_pkey：CREATE UNIQUE INDEX doctor_favorite_template_pkey ON public.doctor_favorite_template USING btree (id)
- idx_fav_doctor_code：CREATE INDEX idx_fav_doctor_code ON public.doctor_favorite_template USING btree (doctor_code)
- idx_fav_template_code：CREATE INDEX idx_fav_template_code ON public.doctor_favorite_template USING btree (template_code)
- uk_doctor_template：CREATE UNIQUE INDEX uk_doctor_template ON public.doctor_favorite_template USING btree (doctor_code, template_code)

### emr_data_element

- 表功能：电子病历数据元定义表，用于描述病历字段的结构、类型和校验规则（来源：数据库注释）
- 表注释：电子病历数据元定义表，用于描述病历字段的结构、类型和校验规则
- 字段数：13

**字段结构与功能**

| 序号 | 字段名 | 类型 | 可空 | 默认值 | 字段功能说明 |
|---:|---|---|---|---|---|
| 1 | id | bigint | 否 | nextval('emr_data_element_id_seq'::regclass) | 主键（来源：数据库注释） |
| 2 | element_code | varchar(64) | 否 | （无） | 数据元编码（全系统唯一）（来源：数据库注释） |
| 3 | element_name | varchar(128) | 否 | （无） | 数据元名称（来源：数据库注释） |
| 4 | element_desc | varchar(256) | 是 | （无） | 数据元说明（来源：数据库注释） |
| 5 | storage_type | varchar(16) | 否 | （无） | 存储数据类型：S，S1，S2.....（来源：数据库注释） |
| 6 | value_format | varchar(16) | 是 | 'NONE'::character varying | 表示格式：A(字母)/N(数字)/AN(字母数字)/NONE(不限制)（来源：数据库注释） |
| 7 | value_length | integer | 是 | （无） | 最大长度或精度限制（来源：数据库注释） |
| 8 | value_domain_code | varchar(64) | 是 | （无） | 值域编码（枚举型数据使用，如性别、血型）（来源：数据库注释） |
| 9 | is_required | boolean | 是 | false | 是否必填（来源：数据库注释） |
| 10 | default_value | varchar(128) | 是 | （无） | 默认值（来源：数据库注释） |
| 11 | enabled | boolean | 是 | true | 是否启用（来源：数据库注释） |
| 12 | created_at | timestamp without time zone | 是 | CURRENT_TIMESTAMP | 创建时间（来源：数据库注释） |
| 13 | updated_at | timestamp without time zone | 是 | CURRENT_TIMESTAMP | 更新时间（来源：数据库注释） |

**关键约束与关系**

- [p] emr_data_element_pkey：PRIMARY KEY (id)
- [u] uq_emr_data_element_code：UNIQUE (element_code)

**索引**

- emr_data_element_pkey：CREATE UNIQUE INDEX emr_data_element_pkey ON public.emr_data_element USING btree (id)
- uq_emr_data_element_code：CREATE UNIQUE INDEX uq_emr_data_element_code ON public.emr_data_element USING btree (element_code)

### emr_data_set

- 表功能：电子病历数据集定义表（多个数据元的集合）（来源：数据库注释）
- 表注释：电子病历数据集定义表（多个数据元的集合）
- 字段数：7

**字段结构与功能**

| 序号 | 字段名 | 类型 | 可空 | 默认值 | 字段功能说明 |
|---:|---|---|---|---|---|
| 1 | id | bigint | 否 | nextval('emr_data_set_id_seq'::regclass) | 主键标识（推断：通用主键命名） |
| 2 | dataset_code | varchar(64) | 否 | （无） | 数据集编码（来源：数据库注释） |
| 3 | dataset_name | varchar(128) | 否 | （无） | 数据集名称（来源：数据库注释） |
| 4 | dataset_desc | varchar(256) | 是 | （无） | 数据集说明（来源：数据库注释） |
| 5 | enabled | boolean | 是 | true | 是否启用（来源：数据库注释） |
| 6 | created_at | timestamp without time zone | 是 | CURRENT_TIMESTAMP | 创建时间（推断：审计字段命名） |
| 7 | updated_at | timestamp without time zone | 是 | CURRENT_TIMESTAMP | 更新时间（推断：审计字段命名） |

**关键约束与关系**

- [p] emr_data_set_pkey：PRIMARY KEY (id)
- [u] uq_emr_dataset_code：UNIQUE (dataset_code)

**索引**

- emr_data_set_pkey：CREATE UNIQUE INDEX emr_data_set_pkey ON public.emr_data_set USING btree (id)
- uq_emr_dataset_code：CREATE UNIQUE INDEX uq_emr_dataset_code ON public.emr_data_set USING btree (dataset_code)

### emr_data_set_element

- 表功能：数据集与数据元关系表（来源：数据库注释）
- 表注释：数据集与数据元关系表
- 字段数：10

**字段结构与功能**

| 序号 | 字段名 | 类型 | 可空 | 默认值 | 字段功能说明 |
|---:|---|---|---|---|---|
| 1 | id | bigint | 否 | nextval('emr_data_set_element_id_seq'::regclass) | 主键标识（推断：通用主键命名） |
| 2 | dataset_code | varchar(64) | 否 | （无） | 数据集编码（来源：数据库注释） |
| 3 | element_code | varchar(64) | 否 | （无） | 数据元编码（来源：数据库注释） |
| 4 | display_order | integer | 是 | 0 | 展示顺序（来源：数据库注释） |
| 5 | is_required | boolean | 是 | false | 是否必填（来源：数据库注释） |
| 6 | is_readonly | boolean | 是 | false | 是否只读（来源：数据库注释） |
| 7 | default_value | varchar(128) | 是 | （无） | 默认值（来源：数据库注释） |
| 8 | created_at | timestamp without time zone | 是 | CURRENT_TIMESTAMP | 创建时间（推断：审计字段命名） |
| 9 | updated_at | timestamp without time zone | 是 | CURRENT_TIMESTAMP | 更新时间（推断：审计字段命名） |
| 10 | element_name | varchar(128) | 否 | ''::character varying | 名称展示字段（推断：字段名包含 name） |

**关键约束与关系**

- [p] emr_data_set_element_pkey：PRIMARY KEY (id)
- [u] uq_dataset_element：UNIQUE (dataset_code, element_code)

**索引**

- emr_data_set_element_pkey：CREATE UNIQUE INDEX emr_data_set_element_pkey ON public.emr_data_set_element USING btree (id)
- uq_dataset_element：CREATE UNIQUE INDEX uq_dataset_element ON public.emr_data_set_element USING btree (dataset_code, element_code)

### emr_drug_dictionary

- 表功能：药品主数据字典表（来源：数据库注释）
- 表注释：药品主数据字典表
- 字段数：10

**字段结构与功能**

| 序号 | 字段名 | 类型 | 可空 | 默认值 | 字段功能说明 |
|---:|---|---|---|---|---|
| 1 | id | bigint | 否 | nextval('emr_drug_dictionary_id_seq'::regclass) | 主键标识（推断：通用主键命名） |
| 2 | drug_code | varchar(64) | 否 | （无） | 药品编码（ATC标准编码）（来源：数据库注释） |
| 3 | drug_name | varchar(256) | 否 | （无） | 药品通用名（来源：数据库注释） |
| 4 | pinyin_initials | varchar(64) | 否 | （无） | 拼音首字母（来源：数据库注释） |
| 5 | dosage_form | varchar(64) | 是 | （无） | 剂型（来源：数据库注释） |
| 6 | specification | varchar(128) | 是 | （无） | 规格（来源：数据库注释） |
| 7 | status | smallint | 否 | 1 | 状态（1启用，0停用）（来源：数据库注释） |
| 8 | sort_no | integer | 否 | 0 | 排序号（来源：数据库注释） |
| 9 | create_time | timestamp without time zone | 是 | CURRENT_TIMESTAMP | 创建时间（推断：审计字段命名） |
| 10 | update_time | timestamp without time zone | 是 | CURRENT_TIMESTAMP | 更新时间（推断：审计字段命名） |

**关键约束与关系**

- [c] ck_emr_drug_dictionary_status：CHECK (status = ANY (ARRAY[0, 1]))
- [p] emr_drug_dictionary_pkey：PRIMARY KEY (id)
- [u] uk_emr_drug_dictionary_code：UNIQUE (drug_code)

**索引**

- emr_drug_dictionary_pkey：CREATE UNIQUE INDEX emr_drug_dictionary_pkey ON public.emr_drug_dictionary USING btree (id)
- idx_emr_drug_dictionary_name：CREATE INDEX idx_emr_drug_dictionary_name ON public.emr_drug_dictionary USING btree (drug_name)
- idx_emr_drug_dictionary_pinyin：CREATE INDEX idx_emr_drug_dictionary_pinyin ON public.emr_drug_dictionary USING btree (pinyin_initials)
- idx_emr_drug_dictionary_status：CREATE INDEX idx_emr_drug_dictionary_status ON public.emr_drug_dictionary USING btree (status)
- uk_emr_drug_dictionary_code：CREATE UNIQUE INDEX uk_emr_drug_dictionary_code ON public.emr_drug_dictionary USING btree (drug_code)

### emr_drug_form_dict

- 表功能：医疗字典主数据表（推断：表名后缀 _dict）
- 表注释：（无）
- 字段数：6

**字段结构与功能**

| 序号 | 字段名 | 类型 | 可空 | 默认值 | 字段功能说明 |
|---:|---|---|---|---|---|
| 1 | form_code | varchar(32) | 否 | （无） | 业务编码/字典编码字段（推断：字段名后缀 _code） |
| 2 | form_name | varchar(64) | 否 | （无） | 名称展示字段（推断：字段名包含 name） |
| 3 | status | smallint | 否 | 1 | 状态字段，用于流程或启停控制（推断：字段名包含 status） |
| 4 | sort_no | integer | 否 | 0 | 待结合业务代码进一步确认（推断：数据库无字段注释） |
| 5 | create_time | timestamp without time zone | 是 | CURRENT_TIMESTAMP | 创建时间（推断：审计字段命名） |
| 6 | update_time | timestamp without time zone | 是 | CURRENT_TIMESTAMP | 更新时间（推断：审计字段命名） |

**关键约束与关系**

- [c] ck_emr_drug_form_status：CHECK (status = ANY (ARRAY[0, 1]))
- [p] emr_drug_form_dict_pkey：PRIMARY KEY (form_code)

**索引**

- emr_drug_form_dict_pkey：CREATE UNIQUE INDEX emr_drug_form_dict_pkey ON public.emr_drug_form_dict USING btree (form_code)

### emr_drug_master

- 表功能：业务/配置表（推断：库中未提供表注释）
- 表注释：（无）
- 字段数：8

**字段结构与功能**

| 序号 | 字段名 | 类型 | 可空 | 默认值 | 字段功能说明 |
|---:|---|---|---|---|---|
| 1 | id | bigint | 否 | nextval('emr_drug_master_id_seq'::regclass) | 主键标识（推断：通用主键命名） |
| 2 | master_code | varchar(64) | 否 | （无） | 业务编码/字典编码字段（推断：字段名后缀 _code） |
| 3 | drug_name | varchar(256) | 否 | （无） | 名称展示字段（推断：字段名包含 name） |
| 4 | pinyin_initials | varchar(64) | 否 | （无） | 待结合业务代码进一步确认（推断：数据库无字段注释） |
| 5 | status | smallint | 否 | 1 | 状态字段，用于流程或启停控制（推断：字段名包含 status） |
| 6 | sort_no | integer | 否 | 0 | 待结合业务代码进一步确认（推断：数据库无字段注释） |
| 7 | create_time | timestamp without time zone | 是 | CURRENT_TIMESTAMP | 创建时间（推断：审计字段命名） |
| 8 | update_time | timestamp without time zone | 是 | CURRENT_TIMESTAMP | 更新时间（推断：审计字段命名） |

**关键约束与关系**

- [c] ck_emr_drug_master_status：CHECK (status = ANY (ARRAY[0, 1]))
- [p] emr_drug_master_pkey：PRIMARY KEY (id)
- [u] uk_emr_drug_master_code：UNIQUE (master_code)
- [u] uk_emr_drug_master_name：UNIQUE (drug_name)

**索引**

- emr_drug_master_pkey：CREATE UNIQUE INDEX emr_drug_master_pkey ON public.emr_drug_master USING btree (id)
- idx_emr_drug_master_name：CREATE INDEX idx_emr_drug_master_name ON public.emr_drug_master USING btree (drug_name)
- idx_emr_drug_master_pinyin：CREATE INDEX idx_emr_drug_master_pinyin ON public.emr_drug_master USING btree (pinyin_initials)
- uk_emr_drug_master_code：CREATE UNIQUE INDEX uk_emr_drug_master_code ON public.emr_drug_master USING btree (master_code)
- uk_emr_drug_master_name：CREATE UNIQUE INDEX uk_emr_drug_master_name ON public.emr_drug_master USING btree (drug_name)

### emr_drug_spec

- 表功能：业务/配置表（推断：库中未提供表注释）
- 表注释：（无）
- 字段数：14

**字段结构与功能**

| 序号 | 字段名 | 类型 | 可空 | 默认值 | 字段功能说明 |
|---:|---|---|---|---|---|
| 1 | id | bigint | 否 | nextval('emr_drug_spec_id_seq'::regclass) | 主键标识（推断：通用主键命名） |
| 2 | spec_code | varchar(80) | 否 | （无） | 业务编码/字典编码字段（推断：字段名后缀 _code） |
| 3 | master_id | bigint | 否 | （无） | 关联实体主键字段（推断：字段名后缀 _id） |
| 4 | form_code | varchar(32) | 否 | （无） | 业务编码/字典编码字段（推断：字段名后缀 _code） |
| 5 | spec_name | varchar(256) | 否 | （无） | 名称展示字段（推断：字段名包含 name） |
| 6 | pack_size | numeric(12,2) | 是 | （无） | 待结合业务代码进一步确认（推断：数据库无字段注释） |
| 7 | min_unit_code | varchar(32) | 否 | （无） | 业务编码/字典编码字段（推断：字段名后缀 _code） |
| 8 | pack_unit_code | varchar(32) | 否 | （无） | 业务编码/字典编码字段（推断：字段名后缀 _code） |
| 9 | dose_value | numeric(12,3) | 是 | （无） | 待结合业务代码进一步确认（推断：数据库无字段注释） |
| 10 | dose_unit_code | varchar(32) | 是 | （无） | 业务编码/字典编码字段（推断：字段名后缀 _code） |
| 11 | status | smallint | 否 | 1 | 状态字段，用于流程或启停控制（推断：字段名包含 status） |
| 12 | sort_no | integer | 否 | 0 | 待结合业务代码进一步确认（推断：数据库无字段注释） |
| 13 | create_time | timestamp without time zone | 是 | CURRENT_TIMESTAMP | 创建时间（推断：审计字段命名） |
| 14 | update_time | timestamp without time zone | 是 | CURRENT_TIMESTAMP | 更新时间（推断：审计字段命名） |

**关键约束与关系**

- [c] ck_emr_drug_spec_status：CHECK (status = ANY (ARRAY[0, 1]))
- [f] fk_emr_drug_spec_dose_unit：FOREIGN KEY (dose_unit_code) REFERENCES emr_drug_unit_dict(unit_code) ON DELETE RESTRICT
- [f] fk_emr_drug_spec_form：FOREIGN KEY (form_code) REFERENCES emr_drug_form_dict(form_code) ON DELETE RESTRICT
- [f] fk_emr_drug_spec_master：FOREIGN KEY (master_id) REFERENCES emr_drug_master(id) ON DELETE RESTRICT
- [f] fk_emr_drug_spec_min_unit：FOREIGN KEY (min_unit_code) REFERENCES emr_drug_unit_dict(unit_code) ON DELETE RESTRICT
- [f] fk_emr_drug_spec_pack_unit：FOREIGN KEY (pack_unit_code) REFERENCES emr_drug_unit_dict(unit_code) ON DELETE RESTRICT
- [p] emr_drug_spec_pkey：PRIMARY KEY (id)
- [u] uk_emr_drug_spec_code：UNIQUE (spec_code)

**索引**

- emr_drug_spec_pkey：CREATE UNIQUE INDEX emr_drug_spec_pkey ON public.emr_drug_spec USING btree (id)
- idx_emr_drug_spec_form：CREATE INDEX idx_emr_drug_spec_form ON public.emr_drug_spec USING btree (form_code)
- idx_emr_drug_spec_master：CREATE INDEX idx_emr_drug_spec_master ON public.emr_drug_spec USING btree (master_id)
- uk_emr_drug_spec_code：CREATE UNIQUE INDEX uk_emr_drug_spec_code ON public.emr_drug_spec USING btree (spec_code)

### emr_drug_unit_dict

- 表功能：医疗字典主数据表（推断：表名后缀 _dict）
- 表注释：（无）
- 字段数：7

**字段结构与功能**

| 序号 | 字段名 | 类型 | 可空 | 默认值 | 字段功能说明 |
|---:|---|---|---|---|---|
| 1 | unit_code | varchar(32) | 否 | （无） | 业务编码/字典编码字段（推断：字段名后缀 _code） |
| 2 | unit_name | varchar(64) | 否 | （无） | 名称展示字段（推断：字段名包含 name） |
| 3 | unit_type | varchar(32) | 否 | 'COUNT'::character varying | 待结合业务代码进一步确认（推断：数据库无字段注释） |
| 4 | status | smallint | 否 | 1 | 状态字段，用于流程或启停控制（推断：字段名包含 status） |
| 5 | sort_no | integer | 否 | 0 | 待结合业务代码进一步确认（推断：数据库无字段注释） |
| 6 | create_time | timestamp without time zone | 是 | CURRENT_TIMESTAMP | 创建时间（推断：审计字段命名） |
| 7 | update_time | timestamp without time zone | 是 | CURRENT_TIMESTAMP | 更新时间（推断：审计字段命名） |

**关键约束与关系**

- [c] ck_emr_drug_unit_status：CHECK (status = ANY (ARRAY[0, 1]))
- [p] emr_drug_unit_dict_pkey：PRIMARY KEY (unit_code)

**索引**

- emr_drug_unit_dict_pkey：CREATE UNIQUE INDEX emr_drug_unit_dict_pkey ON public.emr_drug_unit_dict USING btree (unit_code)

### emr_exam_body_part_dict

- 表功能：医疗字典主数据表（推断：表名后缀 _dict）
- 表注释：（无）
- 字段数：7

**字段结构与功能**

| 序号 | 字段名 | 类型 | 可空 | 默认值 | 字段功能说明 |
|---:|---|---|---|---|---|
| 1 | part_code | varchar(64) | 否 | （无） | 业务编码/字典编码字段（推断：字段名后缀 _code） |
| 2 | part_name | varchar(128) | 否 | （无） | 名称展示字段（推断：字段名包含 name） |
| 3 | pinyin_initials | varchar(64) | 否 | （无） | 待结合业务代码进一步确认（推断：数据库无字段注释） |
| 4 | status | smallint | 否 | 1 | 状态字段，用于流程或启停控制（推断：字段名包含 status） |
| 5 | sort_no | integer | 否 | 0 | 待结合业务代码进一步确认（推断：数据库无字段注释） |
| 6 | create_time | timestamp without time zone | 是 | CURRENT_TIMESTAMP | 创建时间（推断：审计字段命名） |
| 7 | update_time | timestamp without time zone | 是 | CURRENT_TIMESTAMP | 更新时间（推断：审计字段命名） |

**关键约束与关系**

- [c] ck_emr_exam_part_status：CHECK (status = ANY (ARRAY[0, 1]))
- [p] emr_exam_body_part_dict_pkey：PRIMARY KEY (part_code)

**索引**

- emr_exam_body_part_dict_pkey：CREATE UNIQUE INDEX emr_exam_body_part_dict_pkey ON public.emr_exam_body_part_dict USING btree (part_code)

### emr_exam_item_dict

- 表功能：医疗字典主数据表（推断：表名后缀 _dict）
- 表注释：（无）
- 字段数：11

**字段结构与功能**

| 序号 | 字段名 | 类型 | 可空 | 默认值 | 字段功能说明 |
|---:|---|---|---|---|---|
| 1 | id | bigint | 否 | nextval('emr_exam_item_dict_id_seq'::regclass) | 主键标识（推断：通用主键命名） |
| 2 | item_code | varchar(64) | 否 | （无） | 业务编码/字典编码字段（推断：字段名后缀 _code） |
| 3 | item_name | varchar(128) | 否 | （无） | 名称展示字段（推断：字段名包含 name） |
| 4 | pinyin_initials | varchar(64) | 否 | （无） | 待结合业务代码进一步确认（推断：数据库无字段注释） |
| 5 | type_code | varchar(32) | 否 | （无） | 业务编码/字典编码字段（推断：字段名后缀 _code） |
| 6 | supports_part | boolean | 否 | true | 待结合业务代码进一步确认（推断：数据库无字段注释） |
| 7 | supports_enhanced | boolean | 否 | false | 待结合业务代码进一步确认（推断：数据库无字段注释） |
| 8 | status | smallint | 否 | 1 | 状态字段，用于流程或启停控制（推断：字段名包含 status） |
| 9 | sort_no | integer | 否 | 0 | 待结合业务代码进一步确认（推断：数据库无字段注释） |
| 10 | create_time | timestamp without time zone | 是 | CURRENT_TIMESTAMP | 创建时间（推断：审计字段命名） |
| 11 | update_time | timestamp without time zone | 是 | CURRENT_TIMESTAMP | 更新时间（推断：审计字段命名） |

**关键约束与关系**

- [c] ck_emr_exam_item_status：CHECK (status = ANY (ARRAY[0, 1]))
- [f] fk_emr_exam_item_type：FOREIGN KEY (type_code) REFERENCES emr_exam_type_dict(type_code) ON DELETE RESTRICT
- [p] emr_exam_item_dict_pkey：PRIMARY KEY (id)
- [u] uk_emr_exam_item_code：UNIQUE (item_code)

**索引**

- emr_exam_item_dict_pkey：CREATE UNIQUE INDEX emr_exam_item_dict_pkey ON public.emr_exam_item_dict USING btree (id)
- idx_emr_exam_item_name：CREATE INDEX idx_emr_exam_item_name ON public.emr_exam_item_dict USING btree (item_name)
- idx_emr_exam_item_py：CREATE INDEX idx_emr_exam_item_py ON public.emr_exam_item_dict USING btree (pinyin_initials)
- idx_emr_exam_item_type：CREATE INDEX idx_emr_exam_item_type ON public.emr_exam_item_dict USING btree (type_code)
- uk_emr_exam_item_code：CREATE UNIQUE INDEX uk_emr_exam_item_code ON public.emr_exam_item_dict USING btree (item_code)

### emr_exam_item_part_map

- 表功能：主数据映射关系表（推断：表名后缀 _map）
- 表注释：（无）
- 字段数：7

**字段结构与功能**

| 序号 | 字段名 | 类型 | 可空 | 默认值 | 字段功能说明 |
|---:|---|---|---|---|---|
| 1 | id | bigint | 否 | nextval('emr_exam_item_part_map_id_seq'::regclass) | 主键标识（推断：通用主键命名） |
| 2 | item_code | varchar(64) | 否 | （无） | 业务编码/字典编码字段（推断：字段名后缀 _code） |
| 3 | part_code | varchar(64) | 否 | （无） | 业务编码/字典编码字段（推断：字段名后缀 _code） |
| 4 | status | smallint | 否 | 1 | 状态字段，用于流程或启停控制（推断：字段名包含 status） |
| 5 | sort_no | integer | 否 | 0 | 待结合业务代码进一步确认（推断：数据库无字段注释） |
| 6 | create_time | timestamp without time zone | 是 | CURRENT_TIMESTAMP | 创建时间（推断：审计字段命名） |
| 7 | update_time | timestamp without time zone | 是 | CURRENT_TIMESTAMP | 更新时间（推断：审计字段命名） |

**关键约束与关系**

- [c] ck_emr_exam_item_part_status：CHECK (status = ANY (ARRAY[0, 1]))
- [f] fk_emr_exam_item_part_item：FOREIGN KEY (item_code) REFERENCES emr_exam_item_dict(item_code) ON DELETE CASCADE
- [f] fk_emr_exam_item_part_part：FOREIGN KEY (part_code) REFERENCES emr_exam_body_part_dict(part_code) ON DELETE RESTRICT
- [p] emr_exam_item_part_map_pkey：PRIMARY KEY (id)
- [u] uq_emr_exam_item_part：UNIQUE (item_code, part_code)

**索引**

- emr_exam_item_part_map_pkey：CREATE UNIQUE INDEX emr_exam_item_part_map_pkey ON public.emr_exam_item_part_map USING btree (id)
- uq_emr_exam_item_part：CREATE UNIQUE INDEX uq_emr_exam_item_part ON public.emr_exam_item_part_map USING btree (item_code, part_code)

### emr_exam_type_dict

- 表功能：医疗字典主数据表（推断：表名后缀 _dict）
- 表注释：（无）
- 字段数：6

**字段结构与功能**

| 序号 | 字段名 | 类型 | 可空 | 默认值 | 字段功能说明 |
|---:|---|---|---|---|---|
| 1 | type_code | varchar(32) | 否 | （无） | 业务编码/字典编码字段（推断：字段名后缀 _code） |
| 2 | type_name | varchar(64) | 否 | （无） | 名称展示字段（推断：字段名包含 name） |
| 3 | status | smallint | 否 | 1 | 状态字段，用于流程或启停控制（推断：字段名包含 status） |
| 4 | sort_no | integer | 否 | 0 | 待结合业务代码进一步确认（推断：数据库无字段注释） |
| 5 | create_time | timestamp without time zone | 是 | CURRENT_TIMESTAMP | 创建时间（推断：审计字段命名） |
| 6 | update_time | timestamp without time zone | 是 | CURRENT_TIMESTAMP | 更新时间（推断：审计字段命名） |

**关键约束与关系**

- [c] ck_emr_exam_type_status：CHECK (status = ANY (ARRAY[0, 1]))
- [p] emr_exam_type_dict_pkey：PRIMARY KEY (type_code)

**索引**

- emr_exam_type_dict_pkey：CREATE UNIQUE INDEX emr_exam_type_dict_pkey ON public.emr_exam_type_dict USING btree (type_code)

### emr_icd10_diagnosis

- 表功能：ICD-10 疾病诊断字典表（精简版）（来源：数据库注释）
- 表注释：ICD-10 疾病诊断字典表（精简版）
- 字段数：8

**字段结构与功能**

| 序号 | 字段名 | 类型 | 可空 | 默认值 | 字段功能说明 |
|---:|---|---|---|---|---|
| 1 | id | bigint | 否 | nextval('emr_icd10_diagnosis_id_seq'::regclass) | 主键标识（推断：通用主键命名） |
| 2 | diag_code | varchar(32) | 否 | （无） | 疾病诊断编码（ICD-10）（来源：数据库注释） |
| 3 | diag_name | varchar(256) | 否 | （无） | 疾病诊断中文名称（来源：数据库注释） |
| 4 | pinyin_initials | varchar(64) | 否 | （无） | 疾病名称拼音首字母（来源：数据库注释） |
| 5 | status | smallint | 否 | 1 | 状态（1启用，0停用）（来源：数据库注释） |
| 6 | sort_no | integer | 否 | 0 | 排序号（来源：数据库注释） |
| 7 | create_time | timestamp without time zone | 是 | CURRENT_TIMESTAMP | 创建时间（推断：审计字段命名） |
| 8 | update_time | timestamp without time zone | 是 | CURRENT_TIMESTAMP | 更新时间（推断：审计字段命名） |

**关键约束与关系**

- [c] ck_emr_icd10_status：CHECK (status = ANY (ARRAY[0, 1]))
- [p] emr_icd10_diagnosis_pkey：PRIMARY KEY (id)
- [u] uk_emr_icd10_diag_code：UNIQUE (diag_code)

**索引**

- emr_icd10_diagnosis_pkey：CREATE UNIQUE INDEX emr_icd10_diagnosis_pkey ON public.emr_icd10_diagnosis USING btree (id)
- idx_emr_icd10_diag_name：CREATE INDEX idx_emr_icd10_diag_name ON public.emr_icd10_diagnosis USING btree (diag_name)
- idx_emr_icd10_pinyin_initials：CREATE INDEX idx_emr_icd10_pinyin_initials ON public.emr_icd10_diagnosis USING btree (pinyin_initials)
- uk_emr_icd10_diag_code：CREATE UNIQUE INDEX uk_emr_icd10_diag_code ON public.emr_icd10_diagnosis USING btree (diag_code)

### emr_lab_item_dict

- 表功能：医疗字典主数据表（推断：表名后缀 _dict）
- 表注释：（无）
- 字段数：11

**字段结构与功能**

| 序号 | 字段名 | 类型 | 可空 | 默认值 | 字段功能说明 |
|---:|---|---|---|---|---|
| 1 | id | bigint | 否 | nextval('emr_lab_item_dict_id_seq'::regclass) | 主键标识（推断：通用主键命名） |
| 2 | item_code | varchar(64) | 否 | （无） | 业务编码/字典编码字段（推断：字段名后缀 _code） |
| 3 | item_name | varchar(128) | 否 | （无） | 名称展示字段（推断：字段名包含 name） |
| 4 | pinyin_initials | varchar(64) | 否 | （无） | 待结合业务代码进一步确认（推断：数据库无字段注释） |
| 5 | specimen_type | varchar(64) | 是 | （无） | 待结合业务代码进一步确认（推断：数据库无字段注释） |
| 6 | unit | varchar(32) | 是 | （无） | 待结合业务代码进一步确认（推断：数据库无字段注释） |
| 7 | ref_range | varchar(128) | 是 | （无） | 待结合业务代码进一步确认（推断：数据库无字段注释） |
| 8 | status | smallint | 否 | 1 | 状态字段，用于流程或启停控制（推断：字段名包含 status） |
| 9 | sort_no | integer | 否 | 0 | 待结合业务代码进一步确认（推断：数据库无字段注释） |
| 10 | create_time | timestamp without time zone | 是 | CURRENT_TIMESTAMP | 创建时间（推断：审计字段命名） |
| 11 | update_time | timestamp without time zone | 是 | CURRENT_TIMESTAMP | 更新时间（推断：审计字段命名） |

**关键约束与关系**

- [c] ck_emr_lab_item_status：CHECK (status = ANY (ARRAY[0, 1]))
- [p] emr_lab_item_dict_pkey：PRIMARY KEY (id)
- [u] uk_emr_lab_item_code：UNIQUE (item_code)

**索引**

- emr_lab_item_dict_pkey：CREATE UNIQUE INDEX emr_lab_item_dict_pkey ON public.emr_lab_item_dict USING btree (id)
- idx_emr_lab_item_name：CREATE INDEX idx_emr_lab_item_name ON public.emr_lab_item_dict USING btree (item_name)
- idx_emr_lab_item_py：CREATE INDEX idx_emr_lab_item_py ON public.emr_lab_item_dict USING btree (pinyin_initials)
- uk_emr_lab_item_code：CREATE UNIQUE INDEX uk_emr_lab_item_code ON public.emr_lab_item_dict USING btree (item_code)

### emr_template

- 表功能：电子病历模板主表（定义病历结构）（来源：数据库注释）
- 表注释：电子病历模板主表（定义病历结构）
- 字段数：9

**字段结构与功能**

| 序号 | 字段名 | 类型 | 可空 | 默认值 | 字段功能说明 |
|---:|---|---|---|---|---|
| 1 | id | bigint | 否 | nextval('emr_template_id_seq'::regclass) | 模板主键ID（来源：数据库注释） |
| 2 | template_code | varchar(64) | 否 | （无） | 病历模板编码（全局唯一）（来源：数据库注释） |
| 3 | template_name | varchar(128) | 否 | （无） | 病历模板名称（来源：数据库注释） |
| 4 | emr_type | varchar(32) | 否 | （无） | 病历类型（初诊、复诊、病程记录）（来源：数据库注释） |
| 5 | template_scope | varchar(16) | 否 | （无） | 模板范围（HOSPITAL 全院 / DEPARTMENT 科室）（来源：数据库注释） |
| 6 | template_structure | jsonb | 否 | '{}'::jsonb | 病历模板结构定义(JSON，包含数据元占位)（来源：数据库注释） |
| 7 | enabled | boolean | 是 | true | 是否启用（来源：数据库注释） |
| 8 | create_time | timestamp without time zone | 是 | CURRENT_TIMESTAMP | 创建时间（来源：数据库注释） |
| 9 | update_time | timestamp without time zone | 是 | CURRENT_TIMESTAMP | 更新时间（来源：数据库注释） |

**关键约束与关系**

- [c] ck_emr_template_scope：CHECK (template_scope::text = ANY (ARRAY['HOSPITAL'::character varying::text, 'DEPARTMENT'::character varying::text]))
- [p] emr_template_pkey：PRIMARY KEY (id)
- [u] emr_template_template_code_key：UNIQUE (template_code)

**索引**

- emr_template_pkey：CREATE UNIQUE INDEX emr_template_pkey ON public.emr_template USING btree (id)
- emr_template_template_code_key：CREATE UNIQUE INDEX emr_template_template_code_key ON public.emr_template USING btree (template_code)

### emr_template_department

- 表功能：病历模板-科室关联表（适用于科室模板）（来源：数据库注释）
- 表注释：病历模板-科室关联表（适用于科室模板）
- 字段数：5

**字段结构与功能**

| 序号 | 字段名 | 类型 | 可空 | 默认值 | 字段功能说明 |
|---:|---|---|---|---|---|
| 1 | id | bigint | 否 | nextval('emr_template_department_id_seq'::regclass) | 主键ID（来源：数据库注释） |
| 2 | template_id | bigint | 否 | （无） | 病历模板ID（来源：数据库注释） |
| 3 | dept_id | bigint | 否 | （无） | 科室ID（来源：数据库注释） |
| 4 | create_time | timestamp without time zone | 是 | CURRENT_TIMESTAMP | 创建时间（来源：数据库注释） |
| 5 | update_time | timestamp without time zone | 是 | CURRENT_TIMESTAMP | 更新时间（来源：数据库注释） |

**关键约束与关系**

- [f] fk_template_dept_department：FOREIGN KEY (dept_id) REFERENCES sys_department(id)
- [f] fk_template_dept_template：FOREIGN KEY (template_id) REFERENCES emr_template(id) ON DELETE CASCADE
- [p] emr_template_department_pkey：PRIMARY KEY (id)
- [u] uq_template_dept：UNIQUE (template_id, dept_id)

**索引**

- emr_template_department_pkey：CREATE UNIQUE INDEX emr_template_department_pkey ON public.emr_template_department USING btree (id)
- uq_emr_template_department_template_id：CREATE UNIQUE INDEX uq_emr_template_department_template_id ON public.emr_template_department USING btree (template_id)
- uq_template_dept：CREATE UNIQUE INDEX uq_template_dept ON public.emr_template_department USING btree (template_id, dept_id)

### emr_value_domain

- 表功能：电子病历数据元值域定义表，用于描述数据元可选取值范围（来源：数据库注释）
- 表注释：电子病历数据元值域定义表，用于描述数据元可选取值范围
- 字段数：8

**字段结构与功能**

| 序号 | 字段名 | 类型 | 可空 | 默认值 | 字段功能说明 |
|---:|---|---|---|---|---|
| 1 | id | bigint | 否 | nextval('emr_value_domain_id_seq'::regclass) | 主键ID（来源：数据库注释） |
| 2 | domain_code | varchar(64) | 否 | （无） | 值域编码，全系统唯一（来源：数据库注释） |
| 3 | domain_name | varchar(128) | 否 | （无） | 值域名称（来源：数据库注释） |
| 4 | domain_type | varchar(32) | 是 | 'ENUM'::character varying | 值域类型（ENUM 枚举 / RANGE 范围 / REF 引用）（来源：数据库注释） |
| 5 | description | varchar(255) | 是 | （无） | 值域说明（来源：数据库注释） |
| 6 | status | smallint | 是 | 1 | 状态（1 启用，0 停用）（来源：数据库注释） |
| 7 | create_time | timestamp without time zone | 是 | CURRENT_TIMESTAMP | 创建时间（来源：数据库注释） |
| 8 | updated_at | timestamp without time zone | 是 | CURRENT_TIMESTAMP | 更新时间（推断：审计字段命名） |

**关键约束与关系**

- [p] emr_value_domain_pkey：PRIMARY KEY (id)

**索引**

- emr_value_domain_pkey：CREATE UNIQUE INDEX emr_value_domain_pkey ON public.emr_value_domain USING btree (id)
- uk_emr_value_domain_code：CREATE UNIQUE INDEX uk_emr_value_domain_code ON public.emr_value_domain USING btree (domain_code)

### emr_value_domain_item

- 表功能：电子病历数据元值域项表，用于存储值域下的可选取值（来源：数据库注释）
- 表注释：电子病历数据元值域项表，用于存储值域下的可选取值
- 字段数：10

**字段结构与功能**

| 序号 | 字段名 | 类型 | 可空 | 默认值 | 字段功能说明 |
|---:|---|---|---|---|---|
| 1 | id | bigint | 否 | nextval('emr_value_domain_item_id_seq'::regclass) | 主键ID（来源：数据库注释） |
| 2 | domain_code | varchar(64) | 否 | （无） | 所属值域编码，对应数据元的值域定义（来源：数据库注释） |
| 3 | item_code | varchar(64) | 否 | （无） | 值域项编码（来源：数据库注释） |
| 4 | item_name | varchar(128) | 否 | （无） | 值域项名称（来源：数据库注释） |
| 5 | item_order | integer | 是 | 0 | 显示顺序（来源：数据库注释） |
| 6 | is_default | smallint | 是 | 0 | 是否默认值（0 否，1 是）（来源：数据库注释） |
| 7 | status | smallint | 是 | 1 | 状态（1 启用，0 停用）（来源：数据库注释） |
| 8 | remark | varchar(255) | 是 | （无） | 备注说明（来源：数据库注释） |
| 9 | create_time | timestamp without time zone | 是 | CURRENT_TIMESTAMP | 创建时间（来源：数据库注释） |
| 10 | updated_at | timestamp without time zone | 是 | CURRENT_TIMESTAMP | 更新时间（推断：审计字段命名） |

**关键约束与关系**

- [p] emr_value_domain_item_pkey：PRIMARY KEY (id)

**索引**

- emr_value_domain_item_pkey：CREATE UNIQUE INDEX emr_value_domain_item_pkey ON public.emr_value_domain_item USING btree (id)
- uk_emr_value_domain_item_code：CREATE UNIQUE INDEX uk_emr_value_domain_item_code ON public.emr_value_domain_item USING btree (domain_code, item_code)
- uk_emr_value_domain_item_default：CREATE UNIQUE INDEX uk_emr_value_domain_item_default ON public.emr_value_domain_item USING btree (domain_code) WHERE (is_default = 1)

### patient

- 表功能：患者主数据表：存储患者唯一档案（来源：数据库注释）
- 表注释：患者主数据表：存储患者唯一档案
- 字段数：9

**字段结构与功能**

| 序号 | 字段名 | 类型 | 可空 | 默认值 | 字段功能说明 |
|---:|---|---|---|---|---|
| 1 | id | bigint | 否 | nextval('patient_id_seq'::regclass) | 主键ID（来源：数据库注释） |
| 2 | patient_code | varchar(64) | 否 | （无） | 患者唯一编码 (DE02.01.039.00)（来源：数据库注释） |
| 3 | patient_name | varchar(64) | 否 | （无） | 患者姓名（来源：数据库注释） |
| 4 | gender_code | varchar(32) | 是 | （无） | 性别代码 (DE02.01.040.00)（来源：数据库注释） |
| 5 | birthday | integer | 是 | （无） | 出生日期(yyyyMMdd)（来源：数据库注释） |
| 6 | id_card | varchar(18) | 是 | （无） | 身份证号（来源：数据库注释） |
| 7 | phone | varchar(20) | 是 | （无） | 联系电话（来源：数据库注释） |
| 8 | create_time | timestamp without time zone | 是 | CURRENT_TIMESTAMP | 创建时间（来源：数据库注释） |
| 9 | update_time | timestamp without time zone | 是 | CURRENT_TIMESTAMP | 更新时间（来源：数据库注释） |

**关键约束与关系**

- [p] patient_pkey：PRIMARY KEY (id)
- [u] patient_patient_code_key：UNIQUE (patient_code)

**索引**

- patient_patient_code_key：CREATE UNIQUE INDEX patient_patient_code_key ON public.patient USING btree (patient_code)
- patient_pkey：CREATE UNIQUE INDEX patient_pkey ON public.patient USING btree (id)

## 2. 业务数据表

本模块覆盖挂号、门诊就诊、病历、医嘱、检查检验结果等过程数据。

### emr_record

- 表功能：电子病历实例主表（一次就诊可生成多份病历）（来源：数据库注释）
- 表注释：电子病历实例主表（一次就诊可生成多份病历）
- 字段数：11

**字段结构与功能**

| 序号 | 字段名 | 类型 | 可空 | 默认值 | 字段功能说明 |
|---:|---|---|---|---|---|
| 1 | id | bigint | 否 | nextval('emr_record_id_seq'::regclass) | 主键标识（推断：通用主键命名） |
| 2 | emr_code | varchar(64) | 否 | （无） | 病历实例编码（来源：数据库注释） |
| 3 | visit_code | varchar(64) | 否 | （无） | 门诊就诊编码（来源：数据库注释） |
| 4 | patient_code | varchar(64) | 否 | （无） | 患者编码（来源：数据库注释） |
| 5 | doctor_code | varchar(64) | 否 | （无） | 医生编码（来源：数据库注释） |
| 6 | template_code | varchar(64) | 否 | （无） | 病历模板编码（来源：数据库注释） |
| 7 | emr_type | varchar(32) | 否 | （无） | 病历类型（初诊、复诊）（来源：数据库注释） |
| 8 | emr_status | varchar(32) | 是 | 'DRAFT'::character varying | 病历状态（来源：数据库注释） |
| 9 | current_version | integer | 是 | 1 | 当前病历版本号（来源：数据库注释） |
| 10 | create_time | timestamp without time zone | 是 | CURRENT_TIMESTAMP | 创建时间（来源：数据库注释） |
| 11 | update_time | timestamp without time zone | 是 | CURRENT_TIMESTAMP | 更新时间（来源：数据库注释） |

**关键约束与关系**

- [c] ck_emr_record_status：CHECK (emr_status::text = ANY (ARRAY['DRAFT'::character varying::text, 'SIGNED'::character varying::text]))
- [p] emr_record_pkey：PRIMARY KEY (id)
- [u] emr_record_emr_code_key：UNIQUE (emr_code)

**索引**

- emr_record_emr_code_key：CREATE UNIQUE INDEX emr_record_emr_code_key ON public.emr_record USING btree (emr_code)
- emr_record_pkey：CREATE UNIQUE INDEX emr_record_pkey ON public.emr_record USING btree (id)
- idx_emr_record_patient：CREATE INDEX idx_emr_record_patient ON public.emr_record USING btree (patient_code)
- idx_emr_record_visit：CREATE INDEX idx_emr_record_visit ON public.emr_record USING btree (visit_code)
- uq_emr_record_visit_type：CREATE UNIQUE INDEX uq_emr_record_visit_type ON public.emr_record USING btree (visit_code, emr_type)

### emr_record_history

- 表功能：电子病历归档历史表（签署时自动写入，只写不改）（来源：数据库注释）
- 表注释：电子病历归档历史表（签署时自动写入，只写不改）
- 字段数：16

**字段结构与功能**

| 序号 | 字段名 | 类型 | 可空 | 默认值 | 字段功能说明 |
|---:|---|---|---|---|---|
| 1 | id | bigint | 否 | nextval('emr_record_history_id_seq'::regclass) | 主键标识（推断：通用主键命名） |
| 2 | emr_code | varchar(64) | 否 | （无） | 病历实例编码（来源：数据库注释） |
| 3 | visit_code | varchar(64) | 否 | （无） | 门诊就诊编码（来源：数据库注释） |
| 4 | patient_code | varchar(64) | 否 | （无） | 患者编码（来源：数据库注释） |
| 5 | doctor_code | varchar(64) | 否 | （无） | 医生编码（来源：数据库注释） |
| 6 | template_code | varchar(64) | 否 | （无） | 病历模板编码（来源：数据库注释） |
| 7 | emr_type | varchar(32) | 否 | （无） | 病历类型（来源：数据库注释） |
| 8 | version_no | integer | 否 | 1 | 签署时的版本号（来源：数据库注释） |
| 9 | emr_content | jsonb | 否 | '{}'::jsonb | 病历正文（DCWriter XML等）（来源：数据库注释） |
| 10 | encounter_json | jsonb | 否 | '{}'::jsonb | 结构化就诊数据 JSONB（来自 outpatient_encounter_data）（来源：数据库注释） |
| 11 | signed_flag | boolean | 否 | true | 是否已签署（归档时恒为 true）（来源：数据库注释） |
| 12 | signed_at | timestamp without time zone | 否 | CURRENT_TIMESTAMP | 签署时间（来源：数据库注释） |
| 13 | emr_status | varchar(32) | 否 | 'SIGNED'::character varying | 状态字段，用于流程或启停控制（推断：字段名包含 status） |
| 14 | create_time | timestamp without time zone | 否 | CURRENT_TIMESTAMP | 创建时间（推断：审计字段命名） |
| 15 | update_time | timestamp without time zone | 否 | CURRENT_TIMESTAMP | 更新时间（推断：审计字段命名） |
| 16 | archived_at | timestamp without time zone | 否 | CURRENT_TIMESTAMP | 归档时间（来源：数据库注释） |

**关键约束与关系**

- [p] emr_record_history_pkey：PRIMARY KEY (id)
- [u] uq_emr_record_history_emr_code_version：UNIQUE (emr_code, version_no)

**索引**

- emr_record_history_pkey：CREATE UNIQUE INDEX emr_record_history_pkey ON public.emr_record_history USING btree (id)
- idx_emr_record_history_archived_at：CREATE INDEX idx_emr_record_history_archived_at ON public.emr_record_history USING btree (archived_at DESC)
- idx_emr_record_history_patient_code：CREATE INDEX idx_emr_record_history_patient_code ON public.emr_record_history USING btree (patient_code)
- idx_emr_record_history_visit_code：CREATE INDEX idx_emr_record_history_visit_code ON public.emr_record_history USING btree (visit_code)
- uq_emr_record_history_emr_code_version：CREATE UNIQUE INDEX uq_emr_record_history_emr_code_version ON public.emr_record_history USING btree (emr_code, version_no)

### emr_record_version

- 表功能：电子病历版本表（支持多版本、签署冻结）（来源：数据库注释）
- 表注释：电子病历版本表（支持多版本、签署冻结）
- 字段数：7

**字段结构与功能**

| 序号 | 字段名 | 类型 | 可空 | 默认值 | 字段功能说明 |
|---:|---|---|---|---|---|
| 1 | id | bigint | 否 | nextval('emr_record_version_id_seq'::regclass) | 主键标识（推断：通用主键命名） |
| 2 | emr_code | varchar(64) | 否 | （无） | 病历实例编码（来源：数据库注释） |
| 3 | version_no | integer | 否 | （无） | 病历版本号（来源：数据库注释） |
| 4 | emr_content | jsonb | 否 | （无） | 病历内容(JSON)，含医嘱/检查快照（来源：数据库注释） |
| 5 | signed_flag | boolean | 是 | false | 是否已签署（来源：数据库注释） |
| 6 | create_time | timestamp without time zone | 是 | CURRENT_TIMESTAMP | 创建时间（来源：数据库注释） |
| 7 | update_time | timestamp without time zone | 是 | CURRENT_TIMESTAMP | 更新时间（来源：数据库注释） |

**关键约束与关系**

- [p] emr_record_version_pkey：PRIMARY KEY (id)

**索引**

- emr_record_version_pkey：CREATE UNIQUE INDEX emr_record_version_pkey ON public.emr_record_version USING btree (id)
- idx_emr_version_emr：CREATE INDEX idx_emr_version_emr ON public.emr_record_version USING btree (emr_code)
- uk_emr_version：CREATE UNIQUE INDEX uk_emr_version ON public.emr_record_version USING btree (emr_code, version_no)

### exam_result

- 表功能：检查结果表（报告+影像引用）（来源：数据库注释）
- 表注释：检查结果表（报告+影像引用）
- 字段数：13

**字段结构与功能**

| 序号 | 字段名 | 类型 | 可空 | 默认值 | 字段功能说明 |
|---:|---|---|---|---|---|
| 1 | id | bigint | 否 | nextval('exam_result_id_seq'::regclass) | 主键标识（推断：通用主键命名） |
| 2 | order_code | varchar(64) | 否 | （无） | 医嘱编码（来源：数据库注释） |
| 3 | report_content | jsonb | 否 | （无） | 检查报告内容(JSON)（来源：数据库注释） |
| 4 | image_url | varchar(256) | 是 | （无） | 影像存储地址(MinIO)（来源：数据库注释） |
| 5 | report_time | timestamp without time zone | 是 | （无） | 报告完成时间（来源：数据库注释） |
| 6 | create_time | timestamp without time zone | 是 | CURRENT_TIMESTAMP | 创建时间（来源：数据库注释） |
| 7 | update_time | timestamp without time zone | 是 | CURRENT_TIMESTAMP | 更新时间（来源：数据库注释） |
| 8 | report_jsonb | jsonb | 否 | '{}'::jsonb | 结构化 JSON 内容字段（推断：字段语义） |
| 9 | report_xml | text | 否 | ''::text | 待结合业务代码进一步确认（推断：数据库无字段注释） |
| 10 | image_files | jsonb | 否 | '[]'::jsonb | 待结合业务代码进一步确认（推断：数据库无字段注释） |
| 11 | final_diagnosis_jsonb | jsonb | 否 | '{}'::jsonb | 结构化 JSON 内容字段（推断：字段语义） |
| 12 | report_status | varchar(16) | 否 | 'DRAFT'::character varying | 状态字段，用于流程或启停控制（推断：字段名包含 status） |
| 13 | report_doctor_code | varchar(64) | 是 | （无） | 业务编码/字典编码字段（推断：字段名后缀 _code） |

**关键约束与关系**

- [f] fk_exam_res_order：FOREIGN KEY (order_code) REFERENCES medical_order(order_code) ON UPDATE CASCADE ON DELETE CASCADE
- [p] exam_result_pkey：PRIMARY KEY (id)

**索引**

- exam_result_pkey：CREATE UNIQUE INDEX exam_result_pkey ON public.exam_result USING btree (id)
- idx_exam_result_order：CREATE INDEX idx_exam_result_order ON public.exam_result USING btree (order_code)
- uq_exam_result_order_code：CREATE UNIQUE INDEX uq_exam_result_order_code ON public.exam_result USING btree (order_code)

### lab_result

- 表功能：检验结果表（结构化存储）（来源：数据库注释）
- 表注释：检验结果表（结构化存储）
- 字段数：8

**字段结构与功能**

| 序号 | 字段名 | 类型 | 可空 | 默认值 | 字段功能说明 |
|---:|---|---|---|---|---|
| 1 | id | bigint | 否 | nextval('lab_result_id_seq'::regclass) | 主键标识（推断：通用主键命名） |
| 2 | order_code | varchar(64) | 否 | （无） | 医嘱编码（来源：数据库注释） |
| 3 | result_json | jsonb | 否 | （无） | 检验结果(JSON)（来源：数据库注释） |
| 4 | create_time | timestamp without time zone | 是 | CURRENT_TIMESTAMP | 创建时间（来源：数据库注释） |
| 5 | update_time | timestamp without time zone | 是 | CURRENT_TIMESTAMP | 更新时间（来源：数据库注释） |
| 6 | report_status | varchar(16) | 否 | 'DRAFT'::character varying | 状态字段，用于流程或启停控制（推断：字段名包含 status） |
| 7 | report_time | timestamp without time zone | 是 | （无） | 待结合业务代码进一步确认（推断：数据库无字段注释） |
| 8 | report_doctor_code | varchar(64) | 是 | （无） | 业务编码/字典编码字段（推断：字段名后缀 _code） |

**关键约束与关系**

- [f] fk_lab_res_order：FOREIGN KEY (order_code) REFERENCES medical_order(order_code) ON UPDATE CASCADE ON DELETE CASCADE
- [p] lab_result_pkey：PRIMARY KEY (id)

**索引**

- idx_lab_result_order：CREATE INDEX idx_lab_result_order ON public.lab_result USING btree (order_code)
- lab_result_pkey：CREATE UNIQUE INDEX lab_result_pkey ON public.lab_result USING btree (id)
- uq_lab_result_order_code：CREATE UNIQUE INDEX uq_lab_result_order_code ON public.lab_result USING btree (order_code)

### lab_result_item

- 表功能：业务/配置表（推断：库中未提供表注释）
- 表注释：（无）
- 字段数：15

**字段结构与功能**

| 序号 | 字段名 | 类型 | 可空 | 默认值 | 字段功能说明 |
|---:|---|---|---|---|---|
| 1 | id | bigint | 否 | nextval('lab_result_item_id_seq'::regclass) | 主键标识（推断：通用主键命名） |
| 2 | order_code | varchar(64) | 否 | （无） | 业务编码/字典编码字段（推断：字段名后缀 _code） |
| 3 | lab_item_code | varchar(64) | 否 | （无） | 业务编码/字典编码字段（推断：字段名后缀 _code） |
| 4 | item_name | varchar(128) | 否 | （无） | 名称展示字段（推断：字段名包含 name） |
| 5 | result_value_num | numeric(12,4) | 是 | （无） | 待结合业务代码进一步确认（推断：数据库无字段注释） |
| 6 | result_value_text | varchar(128) | 是 | （无） | 待结合业务代码进一步确认（推断：数据库无字段注释） |
| 7 | unit | varchar(32) | 是 | （无） | 待结合业务代码进一步确认（推断：数据库无字段注释） |
| 8 | ref_low | numeric(12,4) | 是 | （无） | 待结合业务代码进一步确认（推断：数据库无字段注释） |
| 9 | ref_high | numeric(12,4) | 是 | （无） | 待结合业务代码进一步确认（推断：数据库无字段注释） |
| 10 | ref_text | varchar(128) | 是 | （无） | 待结合业务代码进一步确认（推断：数据库无字段注释） |
| 11 | critical_low | numeric(12,4) | 是 | （无） | 待结合业务代码进一步确认（推断：数据库无字段注释） |
| 12 | critical_high | numeric(12,4) | 是 | （无） | 待结合业务代码进一步确认（推断：数据库无字段注释） |
| 13 | result_flag | varchar(2) | 否 | 'N'::character varying | 待结合业务代码进一步确认（推断：数据库无字段注释） |
| 14 | create_time | timestamp without time zone | 是 | CURRENT_TIMESTAMP | 创建时间（推断：审计字段命名） |
| 15 | update_time | timestamp without time zone | 是 | CURRENT_TIMESTAMP | 更新时间（推断：审计字段命名） |

**关键约束与关系**

- [f] fk_lab_result_item_order：FOREIGN KEY (order_code) REFERENCES medical_order(order_code) ON UPDATE CASCADE ON DELETE CASCADE
- [p] lab_result_item_pkey：PRIMARY KEY (id)

**索引**

- idx_lab_result_item_order：CREATE INDEX idx_lab_result_item_order ON public.lab_result_item USING btree (order_code)
- lab_result_item_pkey：CREATE UNIQUE INDEX lab_result_item_pkey ON public.lab_result_item USING btree (id)

### medical_order

- 表功能：医嘱主表（统一医嘱生命周期管理）（来源：数据库注释）
- 表注释：医嘱主表（统一医嘱生命周期管理）
- 字段数：14

**字段结构与功能**

| 序号 | 字段名 | 类型 | 可空 | 默认值 | 字段功能说明 |
|---:|---|---|---|---|---|
| 1 | id | bigint | 否 | nextval('medical_order_id_seq'::regclass) | 主键标识（推断：通用主键命名） |
| 2 | order_code | varchar(64) | 否 | （无） | 医嘱编码（来源：数据库注释） |
| 3 | visit_code | varchar(64) | 否 | （无） | 就诊编码（来源：数据库注释） |
| 4 | patient_code | varchar(64) | 否 | （无） | 患者编码（来源：数据库注释） |
| 5 | doctor_code | varchar(64) | 否 | （无） | 医生编码（来源：数据库注释） |
| 6 | order_type | order_type_enum | 否 | （无） | 医嘱类型（药品/检查/检验）（来源：数据库注释） |
| 7 | order_status | order_status_enum | 否 | （无） | 医嘱状态（来源：数据库注释） |
| 8 | create_time | timestamp without time zone | 是 | CURRENT_TIMESTAMP | 创建时间（来源：数据库注释） |
| 9 | update_time | timestamp without time zone | 是 | CURRENT_TIMESTAMP | 更新时间（来源：数据库注释） |
| 10 | status_version | bigint | 否 | 0 | 待结合业务代码进一步确认（推断：数据库无字段注释） |
| 11 | received_time | timestamp without time zone | 是 | （无） | 待结合业务代码进一步确认（推断：数据库无字段注释） |
| 12 | completed_time | timestamp without time zone | 是 | （无） | 待结合业务代码进一步确认（推断：数据库无字段注释） |
| 13 | cancelled_time | timestamp without time zone | 是 | （无） | 待结合业务代码进一步确认（推断：数据库无字段注释） |
| 14 | cancel_reason | text | 是 | （无） | 待结合业务代码进一步确认（推断：数据库无字段注释） |

**关键约束与关系**

- [p] medical_order_pkey：PRIMARY KEY (id)
- [u] medical_order_order_code_key：UNIQUE (order_code)

**索引**

- idx_order_patient：CREATE INDEX idx_order_patient ON public.medical_order USING btree (patient_code)
- idx_order_visit：CREATE INDEX idx_order_visit ON public.medical_order USING btree (visit_code)
- medical_order_order_code_key：CREATE UNIQUE INDEX medical_order_order_code_key ON public.medical_order USING btree (order_code)
- medical_order_pkey：CREATE UNIQUE INDEX medical_order_pkey ON public.medical_order USING btree (id)

### medical_order_drug_item

- 表功能：医嘱闭环业务数据表（推断：medical_order 前缀）
- 表注释：（无）
- 字段数：18

**字段结构与功能**

| 序号 | 字段名 | 类型 | 可空 | 默认值 | 字段功能说明 |
|---:|---|---|---|---|---|
| 1 | id | bigint | 否 | nextval('medical_order_drug_item_id_seq'::regclass) | 主键标识（推断：通用主键命名） |
| 2 | order_code | varchar(64) | 否 | （无） | 业务编码/字典编码字段（推断：字段名后缀 _code） |
| 3 | item_no | integer | 否 | （无） | 待结合业务代码进一步确认（推断：数据库无字段注释） |
| 4 | drug_master_id | bigint | 是 | （无） | 关联实体主键字段（推断：字段名后缀 _id） |
| 5 | drug_spec_id | bigint | 是 | （无） | 关联实体主键字段（推断：字段名后缀 _id） |
| 6 | drug_code | varchar(64) | 是 | （无） | 业务编码/字典编码字段（推断：字段名后缀 _code） |
| 7 | drug_name_snapshot | varchar(128) | 否 | （无） | 待结合业务代码进一步确认（推断：数据库无字段注释） |
| 8 | spec_snapshot | varchar(256) | 是 | （无） | 待结合业务代码进一步确认（推断：数据库无字段注释） |
| 9 | dose_value | numeric(10,2) | 是 | （无） | 待结合业务代码进一步确认（推断：数据库无字段注释） |
| 10 | dose_unit_code | varchar(32) | 是 | （无） | 业务编码/字典编码字段（推断：字段名后缀 _code） |
| 11 | qty_value | numeric(10,2) | 是 | （无） | 待结合业务代码进一步确认（推断：数据库无字段注释） |
| 12 | qty_unit_code | varchar(32) | 是 | （无） | 业务编码/字典编码字段（推断：字段名后缀 _code） |
| 13 | frequency_code | varchar(32) | 是 | （无） | 业务编码/字典编码字段（推断：字段名后缀 _code） |
| 14 | route_code | varchar(32) | 是 | （无） | 业务编码/字典编码字段（推断：字段名后缀 _code） |
| 15 | duration_days | integer | 是 | （无） | 待结合业务代码进一步确认（推断：数据库无字段注释） |
| 16 | remark | text | 是 | （无） | 备注/描述说明字段（推断：语义命名） |
| 17 | create_time | timestamp without time zone | 是 | CURRENT_TIMESTAMP | 创建时间（推断：审计字段命名） |
| 18 | update_time | timestamp without time zone | 是 | CURRENT_TIMESTAMP | 更新时间（推断：审计字段命名） |

**关键约束与关系**

- [f] fk_medical_order_drug_item_order：FOREIGN KEY (order_code) REFERENCES medical_order(order_code) ON UPDATE CASCADE ON DELETE CASCADE
- [p] medical_order_drug_item_pkey：PRIMARY KEY (id)
- [u] uq_medical_order_drug_item：UNIQUE (order_code, item_no)

**索引**

- idx_medical_order_drug_item_order：CREATE INDEX idx_medical_order_drug_item_order ON public.medical_order_drug_item USING btree (order_code)
- medical_order_drug_item_pkey：CREATE UNIQUE INDEX medical_order_drug_item_pkey ON public.medical_order_drug_item USING btree (id)
- uq_medical_order_drug_item：CREATE UNIQUE INDEX uq_medical_order_drug_item ON public.medical_order_drug_item USING btree (order_code, item_no)

### medical_order_exam

- 表功能：检查医嘱明细表（来源：数据库注释）
- 表注释：检查医嘱明细表
- 字段数：14

**字段结构与功能**

| 序号 | 字段名 | 类型 | 可空 | 默认值 | 字段功能说明 |
|---:|---|---|---|---|---|
| 1 | id | bigint | 否 | nextval('medical_order_exam_id_seq'::regclass) | 主键标识（推断：通用主键命名） |
| 2 | order_code | varchar(64) | 否 | （无） | 医嘱编码（来源：数据库注释） |
| 3 | exam_item_code | varchar(64) | 否 | （无） | 检查项目编码（来源：数据库注释） |
| 4 | exam_item_name | varchar(128) | 否 | （无） | 检查项目名称（来源：数据库注释） |
| 5 | exam_part | varchar(128) | 是 | （无） | 检查部位（来源：数据库注释） |
| 6 | is_enhanced | boolean | 是 | （无） | 是否增强（来源：数据库注释） |
| 7 | clinical_purpose | text | 是 | （无） | 临床目的（来源：数据库注释） |
| 8 | create_time | timestamp without time zone | 是 | CURRENT_TIMESTAMP | 创建时间（来源：数据库注释） |
| 9 | update_time | timestamp without time zone | 是 | CURRENT_TIMESTAMP | 更新时间（来源：数据库注释） |
| 10 | exam_item_id | bigint | 是 | （无） | 关联实体主键字段（推断：字段名后缀 _id） |
| 11 | exam_type_code | varchar(32) | 是 | （无） | 业务编码/字典编码字段（推断：字段名后缀 _code） |
| 12 | supports_part | boolean | 是 | （无） | 待结合业务代码进一步确认（推断：数据库无字段注释） |
| 13 | supports_enhanced | boolean | 是 | （无） | 待结合业务代码进一步确认（推断：数据库无字段注释） |
| 14 | exam_item_snapshot | varchar(256) | 是 | （无） | 待结合业务代码进一步确认（推断：数据库无字段注释） |

**关键约束与关系**

- [f] fk_exam_order：FOREIGN KEY (order_code) REFERENCES medical_order(order_code) ON UPDATE CASCADE ON DELETE CASCADE
- [f] fk_medical_order_exam_item_id：FOREIGN KEY (exam_item_id) REFERENCES emr_exam_item_dict(id)
- [p] medical_order_exam_pkey：PRIMARY KEY (id)

**索引**

- idx_exam_order：CREATE INDEX idx_exam_order ON public.medical_order_exam USING btree (order_code)
- medical_order_exam_pkey：CREATE UNIQUE INDEX medical_order_exam_pkey ON public.medical_order_exam USING btree (id)

### medical_order_exam_part

- 表功能：医嘱闭环业务数据表（推断：medical_order 前缀）
- 表注释：（无）
- 字段数：7

**字段结构与功能**

| 序号 | 字段名 | 类型 | 可空 | 默认值 | 字段功能说明 |
|---:|---|---|---|---|---|
| 1 | id | bigint | 否 | nextval('medical_order_exam_part_id_seq'::regclass) | 主键标识（推断：通用主键命名） |
| 2 | order_exam_id | bigint | 否 | （无） | 关联实体主键字段（推断：字段名后缀 _id） |
| 3 | part_code | varchar(64) | 否 | （无） | 业务编码/字典编码字段（推断：字段名后缀 _code） |
| 4 | part_name | varchar(128) | 否 | （无） | 名称展示字段（推断：字段名包含 name） |
| 5 | sort_no | integer | 否 | 0 | 待结合业务代码进一步确认（推断：数据库无字段注释） |
| 6 | create_time | timestamp without time zone | 是 | CURRENT_TIMESTAMP | 创建时间（推断：审计字段命名） |
| 7 | update_time | timestamp without time zone | 是 | CURRENT_TIMESTAMP | 更新时间（推断：审计字段命名） |

**关键约束与关系**

- [f] fk_medical_order_exam_part_dict：FOREIGN KEY (part_code) REFERENCES emr_exam_body_part_dict(part_code)
- [f] fk_medical_order_exam_part_exam：FOREIGN KEY (order_exam_id) REFERENCES medical_order_exam(id) ON DELETE CASCADE
- [p] medical_order_exam_part_pkey：PRIMARY KEY (id)
- [u] uq_medical_order_exam_part：UNIQUE (order_exam_id, part_code)

**索引**

- idx_medical_order_exam_part_code：CREATE INDEX idx_medical_order_exam_part_code ON public.medical_order_exam_part USING btree (part_code)
- idx_medical_order_exam_part_exam：CREATE INDEX idx_medical_order_exam_part_exam ON public.medical_order_exam_part USING btree (order_exam_id)
- medical_order_exam_part_pkey：CREATE UNIQUE INDEX medical_order_exam_part_pkey ON public.medical_order_exam_part USING btree (id)
- uq_medical_order_exam_part：CREATE UNIQUE INDEX uq_medical_order_exam_part ON public.medical_order_exam_part USING btree (order_exam_id, part_code)

### medical_order_lab_item

- 表功能：医嘱闭环业务数据表（推断：medical_order 前缀）
- 表注释：（无）
- 字段数：13

**字段结构与功能**

| 序号 | 字段名 | 类型 | 可空 | 默认值 | 字段功能说明 |
|---:|---|---|---|---|---|
| 1 | id | bigint | 否 | nextval('medical_order_lab_item_id_seq'::regclass) | 主键标识（推断：通用主键命名） |
| 2 | order_code | varchar(64) | 否 | （无） | 业务编码/字典编码字段（推断：字段名后缀 _code） |
| 3 | item_no | integer | 否 | （无） | 待结合业务代码进一步确认（推断：数据库无字段注释） |
| 4 | lab_item_code | varchar(64) | 否 | （无） | 业务编码/字典编码字段（推断：字段名后缀 _code） |
| 5 | lab_item_name_snapshot | varchar(128) | 否 | （无） | 待结合业务代码进一步确认（推断：数据库无字段注释） |
| 6 | specimen_type | varchar(64) | 是 | （无） | 待结合业务代码进一步确认（推断：数据库无字段注释） |
| 7 | unit | varchar(32) | 是 | （无） | 待结合业务代码进一步确认（推断：数据库无字段注释） |
| 8 | ref_low | numeric(12,4) | 是 | （无） | 待结合业务代码进一步确认（推断：数据库无字段注释） |
| 9 | ref_high | numeric(12,4) | 是 | （无） | 待结合业务代码进一步确认（推断：数据库无字段注释） |
| 10 | ref_text | varchar(128) | 是 | （无） | 待结合业务代码进一步确认（推断：数据库无字段注释） |
| 11 | clinical_diagnosis | text | 是 | （无） | 待结合业务代码进一步确认（推断：数据库无字段注释） |
| 12 | create_time | timestamp without time zone | 是 | CURRENT_TIMESTAMP | 创建时间（推断：审计字段命名） |
| 13 | update_time | timestamp without time zone | 是 | CURRENT_TIMESTAMP | 更新时间（推断：审计字段命名） |

**关键约束与关系**

- [f] fk_medical_order_lab_item_order：FOREIGN KEY (order_code) REFERENCES medical_order(order_code) ON UPDATE CASCADE ON DELETE CASCADE
- [p] medical_order_lab_item_pkey：PRIMARY KEY (id)
- [u] uq_medical_order_lab_item：UNIQUE (order_code, item_no)

**索引**

- idx_medical_order_lab_item_order：CREATE INDEX idx_medical_order_lab_item_order ON public.medical_order_lab_item USING btree (order_code)
- medical_order_lab_item_pkey：CREATE UNIQUE INDEX medical_order_lab_item_pkey ON public.medical_order_lab_item USING btree (id)
- uq_medical_order_lab_item：CREATE UNIQUE INDEX uq_medical_order_lab_item ON public.medical_order_lab_item USING btree (order_code, item_no)

### outpatient_encounter_data

- 表功能：门诊就诊过程事实表：存储本次就诊（初诊或复诊）的独立事实数据（来源：数据库注释）
- 表注释：门诊就诊过程事实表：存储本次就诊（初诊或复诊）的独立事实数据
- 字段数：5

**字段结构与功能**

| 序号 | 字段名 | 类型 | 可空 | 默认值 | 字段功能说明 |
|---:|---|---|---|---|---|
| 1 | id | bigint | 否 | nextval('outpatient_encounter_data_id_seq'::regclass) | 主键ID（来源：数据库注释） |
| 2 | visit_code | varchar(64) | 否 | （无） | 就诊编码：1:1 绑定单次就诊（来源：数据库注释） |
| 3 | encounter_json | jsonb | 否 | （无） | 结构化诊疗数据 (DE 编码集合)（来源：数据库注释） |
| 4 | create_time | timestamp without time zone | 是 | CURRENT_TIMESTAMP | 创建时间（来源：数据库注释） |
| 5 | update_time | timestamp without time zone | 是 | CURRENT_TIMESTAMP | 更新时间（来源：数据库注释） |

**关键约束与关系**

- [f] fk_encounter_visit：FOREIGN KEY (visit_code) REFERENCES outpatient_visit_active(visit_code) ON DELETE CASCADE
- [p] outpatient_encounter_data_pkey：PRIMARY KEY (id)
- [u] outpatient_encounter_data_visit_code_key：UNIQUE (visit_code)

**索引**

- idx_encounter_json_gin：CREATE INDEX idx_encounter_json_gin ON public.outpatient_encounter_data USING gin (encounter_json)
- outpatient_encounter_data_pkey：CREATE UNIQUE INDEX outpatient_encounter_data_pkey ON public.outpatient_encounter_data USING btree (id)
- outpatient_encounter_data_visit_code_key：CREATE UNIQUE INDEX outpatient_encounter_data_visit_code_key ON public.outpatient_encounter_data USING btree (visit_code)

### outpatient_visit_active

- 表功能：门诊就诊主表：记录单次诊疗行为，区分初复诊类别（来源：数据库注释）
- 表注释：门诊就诊主表：记录单次诊疗行为，区分初复诊类别
- 字段数：12

**字段结构与功能**

| 序号 | 字段名 | 类型 | 可空 | 默认值 | 字段功能说明 |
|---:|---|---|---|---|---|
| 1 | id | bigint | 否 | nextval('outpatient_visit_id_seq'::regclass) | 主键ID（来源：数据库注释） |
| 2 | visit_code | varchar(64) | 否 | （无） | 就诊编码：单次诊疗所有事实数据的根键（来源：数据库注释） |
| 3 | reg_code | varchar(64) | 否 | （无） | 关联挂号单 (1:1)（来源：数据库注释） |
| 4 | patient_code | varchar(64) | 否 | （无） | 患者编码（来源：数据库注释） |
| 5 | dept_code | varchar(64) | 否 | （无） | 接诊科室（来源：数据库注释） |
| 6 | doctor_code | varchar(64) | 否 | （无） | 接诊医生（来源：数据库注释） |
| 7 | visit_type | varchar(32) | 是 | 'FIRST'::character varying | 诊疗类型：FIRST-初诊, FOLLOW_UP-复诊 (DE06.00.062.00)（来源：数据库注释） |
| 8 | visit_status | visit_status_enum | 否 | 'REGISTERED'::visit_status_enum | 就诊状态（来源：数据库注释） |
| 9 | start_time | timestamp without time zone | 是 | CURRENT_TIMESTAMP | 接诊开始时间（来源：数据库注释） |
| 10 | end_time | timestamp without time zone | 是 | （无） | 接诊完成时间（来源：数据库注释） |
| 11 | create_time | timestamp without time zone | 是 | CURRENT_TIMESTAMP | 创建时间（来源：数据库注释） |
| 12 | update_time | timestamp without time zone | 是 | CURRENT_TIMESTAMP | 更新时间（来源：数据库注释） |

**关键约束与关系**

- [f] fk_visit_dept：FOREIGN KEY (dept_code) REFERENCES sys_department(dept_code)
- [f] fk_visit_patient：FOREIGN KEY (patient_code) REFERENCES patient(patient_code)
- [f] fk_visit_reg：FOREIGN KEY (reg_code) REFERENCES registration(reg_code)
- [p] outpatient_visit_pkey：PRIMARY KEY (id)
- [u] outpatient_visit_reg_code_key：UNIQUE (reg_code)
- [u] outpatient_visit_visit_code_key：UNIQUE (visit_code)

**索引**

- outpatient_visit_pkey：CREATE UNIQUE INDEX outpatient_visit_pkey ON public.outpatient_visit_active USING btree (id)
- outpatient_visit_reg_code_key：CREATE UNIQUE INDEX outpatient_visit_reg_code_key ON public.outpatient_visit_active USING btree (reg_code)
- outpatient_visit_visit_code_key：CREATE UNIQUE INDEX outpatient_visit_visit_code_key ON public.outpatient_visit_active USING btree (visit_code)

### outpatient_visit_history

- 表功能：门诊流程业务数据表（推断：outpatient 前缀）
- 表注释：（无）
- 字段数：15

**字段结构与功能**

| 序号 | 字段名 | 类型 | 可空 | 默认值 | 字段功能说明 |
|---:|---|---|---|---|---|
| 1 | id | bigint | 否 | nextval('outpatient_visit_history_id_seq'::regclass) | 主键标识（推断：通用主键命名） |
| 2 | visit_code | varchar(64) | 否 | （无） | 业务编码/字典编码字段（推断：字段名后缀 _code） |
| 3 | reg_code | varchar(64) | 否 | （无） | 业务编码/字典编码字段（推断：字段名后缀 _code） |
| 4 | patient_code | varchar(64) | 否 | （无） | 业务编码/字典编码字段（推断：字段名后缀 _code） |
| 5 | dept_code | varchar(64) | 否 | （无） | 业务编码/字典编码字段（推断：字段名后缀 _code） |
| 6 | doctor_code | varchar(64) | 否 | （无） | 业务编码/字典编码字段（推断：字段名后缀 _code） |
| 7 | visit_type | varchar(32) | 是 | 'FIRST'::character varying | 待结合业务代码进一步确认（推断：数据库无字段注释） |
| 8 | visit_status | visit_status_enum | 否 | （无） | 状态字段，用于流程或启停控制（推断：字段名包含 status） |
| 9 | start_time | timestamp without time zone | 是 | （无） | 待结合业务代码进一步确认（推断：数据库无字段注释） |
| 10 | end_time | timestamp without time zone | 是 | （无） | 待结合业务代码进一步确认（推断：数据库无字段注释） |
| 11 | create_time | timestamp without time zone | 是 | （无） | 创建时间（推断：审计字段命名） |
| 12 | update_time | timestamp without time zone | 是 | （无） | 更新时间（推断：审计字段命名） |
| 13 | encounter_json | jsonb | 否 | '{}'::jsonb | 结构化 JSON 内容字段（推断：字段语义） |
| 14 | archived_at | timestamp without time zone | 否 | CURRENT_TIMESTAMP | 待结合业务代码进一步确认（推断：数据库无字段注释） |
| 15 | archive_reason | varchar(32) | 否 | （无） | 待结合业务代码进一步确认（推断：数据库无字段注释） |

**关键约束与关系**

- [p] outpatient_visit_history_pkey：PRIMARY KEY (id)
- [u] outpatient_visit_history_visit_code_key：UNIQUE (visit_code)

**索引**

- idx_visit_history_archived_at：CREATE INDEX idx_visit_history_archived_at ON public.outpatient_visit_history USING btree (archived_at DESC)
- idx_visit_history_patient_code：CREATE INDEX idx_visit_history_patient_code ON public.outpatient_visit_history USING btree (patient_code)
- idx_visit_history_reg_code：CREATE INDEX idx_visit_history_reg_code ON public.outpatient_visit_history USING btree (reg_code)
- outpatient_visit_history_pkey：CREATE UNIQUE INDEX outpatient_visit_history_pkey ON public.outpatient_visit_history USING btree (id)
- outpatient_visit_history_visit_code_key：CREATE UNIQUE INDEX outpatient_visit_history_visit_code_key ON public.outpatient_visit_history USING btree (visit_code)

### registration

- 表功能：门诊挂号表：单次就诊的号源凭证，每次就诊（含复诊）均需新记录（来源：数据库注释）
- 表注释：门诊挂号表：单次就诊的号源凭证，每次就诊（含复诊）均需新记录
- 字段数：11

**字段结构与功能**

| 序号 | 字段名 | 类型 | 可空 | 默认值 | 字段功能说明 |
|---:|---|---|---|---|---|
| 1 | id | bigint | 否 | nextval('registration_id_seq'::regclass) | 主键ID（来源：数据库注释） |
| 2 | reg_code | varchar(64) | 否 | gen_reg_code() | 挂号流水号：本次就诊的唯一入场券（来源：数据库注释） |
| 3 | patient_code | varchar(64) | 否 | （无） | 患者编码（来源：数据库注释） |
| 4 | dept_code | varchar(64) | 否 | （无） | 挂号科室编码（来源：数据库注释） |
| 5 | doctor_code | varchar(64) | 否 | （无） | 挂号医生编码（来源：数据库注释） |
| 6 | reg_date | integer | 否 | （无） | 计划就诊日期(yyyyMMdd)（来源：数据库注释） |
| 7 | time_slot | varchar(16) | 是 | （无） | 就诊时段：AM/PM（来源：数据库注释） |
| 8 | seq_no | integer | 是 | （无） | 排队号序（来源：数据库注释） |
| 9 | reg_status | varchar(32) | 是 | 'REGISTERED'::character varying | 挂号状态（来源：数据库注释） |
| 10 | create_time | timestamp without time zone | 是 | CURRENT_TIMESTAMP | 创建时间（来源：数据库注释） |
| 11 | update_time | timestamp without time zone | 是 | CURRENT_TIMESTAMP | 更新时间（来源：数据库注释） |

**关键约束与关系**

- [f] fk_reg_dept：FOREIGN KEY (dept_code) REFERENCES sys_department(dept_code)
- [f] fk_reg_patient：FOREIGN KEY (patient_code) REFERENCES patient(patient_code)
- [p] registration_pkey：PRIMARY KEY (id)
- [u] registration_reg_code_key：UNIQUE (reg_code)

**索引**

- registration_pkey：CREATE UNIQUE INDEX registration_pkey ON public.registration USING btree (id)
- registration_reg_code_key：CREATE UNIQUE INDEX registration_reg_code_key ON public.registration USING btree (reg_code)

## 3. 视图

### 3.1 视图清单与可更新性

| 视图名 | is_updatable | is_insertable_into | check_option | INSTEAD OF 触发器 | 更新策略说明 |
|---|---|---|---|---|---|
| v_doctor_user_manage | NO | NO | NONE | trg_v_doctor_user_manage_iud -> fn_v_doctor_user_manage_iud | 通过 INSTEAD OF 触发器转写到底层表；非自动可更新。 |
| v_emr_template_manage | NO | NO | NONE | trg_v_emr_template_manage_iou -> fn_v_emr_template_manage_iou | 通过 INSTEAD OF 触发器转写到底层表；非自动可更新。 |
| v_emr_template_scope | NO | NO | NONE | （无） | 只读视图：无 INSTEAD OF 触发器，且含连接/聚合/表达式。 |
| v_outpatient_emr_bundle | NO | NO | NONE | trg_v_outpatient_emr_bundle_u -> fn_v_outpatient_emr_bundle_u | 通过 INSTEAD OF 触发器转写到底层表；非自动可更新。 |
| v_outpatient_emr_bundle_latest | NO | NO | NONE | （无） | 只读视图：无 INSTEAD OF 触发器，且含连接/聚合/表达式。 |
| v_outpatient_visit_detail | NO | NO | NONE | （无） | 只读视图：无 INSTEAD OF 触发器，且含连接/聚合/表达式。 |

### 3.2 视图：v_doctor_user_manage

**字段结构**

| 序号 | 字段名 | 类型 | 字段功能说明 |
|---:|---|---|---|
| 1 | id | bigint | 记录标识字段（推断） |
| 2 | doctor_id | bigint | 关联标识字段（推断） |
| 3 | user_id | bigint | 关联标识字段（推断） |
| 4 | doctor_code | varchar(64) | 业务编码字段（推断） |
| 5 | doctor_name | varchar(128) | 名称展示字段（推断） |
| 6 | dept_code | varchar(64) | 业务编码字段（推断） |
| 7 | title_code | varchar(32) | 业务编码字段（推断） |
| 8 | license_no | varchar(64) | 视图输出字段，语义来自底层查询定义（推断） |
| 9 | username | varchar(50) | 视图输出字段，语义来自底层查询定义（推断） |
| 10 | password | varchar(100) | 视图输出字段，语义来自底层查询定义（推断） |
| 11 | doctor_enabled | boolean | 视图输出字段，语义来自底层查询定义（推断） |
| 12 | user_status | integer | 视图输出字段，语义来自底层查询定义（推断） |
| 13 | user_is_deleted | integer | 视图输出字段，语义来自底层查询定义（推断） |
| 14 | doctor_create_time | timestamp without time zone | 视图输出字段，语义来自底层查询定义（推断） |
| 15 | doctor_update_time | timestamp without time zone | 视图输出字段，语义来自底层查询定义（推断） |
| 16 | user_create_time | timestamp without time zone | 视图输出字段，语义来自底层查询定义（推断） |
| 17 | user_update_time | timestamp without time zone | 视图输出字段，语义来自底层查询定义（推断） |

**定义 SQL（数据库当前定义）**

```sql
 SELECT d.id,
    d.id AS doctor_id,
    u.id AS user_id,
    d.doctor_code,
    d.doctor_name,
    d.dept_code,
    d.title_code,
    d.license_no,
    d.user_username AS username,
    NULL::character varying(100) AS password,
    d.enabled AS doctor_enabled,
    u.status AS user_status,
    u.is_deleted AS user_is_deleted,
    d.create_time AS doctor_create_time,
    d.update_time AS doctor_update_time,
    u.create_time AS user_create_time,
    u.update_time AS user_update_time
   FROM (doctor d
     JOIN sys_user u ON (((u.username)::text = (d.user_username)::text)));
```


### 3.2 视图：v_emr_template_manage

**字段结构**

| 序号 | 字段名 | 类型 | 字段功能说明 |
|---:|---|---|---|
| 1 | id | bigint | 记录标识字段（推断） |
| 2 | template_code | varchar(64) | 业务编码字段（推断） |
| 3 | template_name | varchar(128) | 名称展示字段（推断） |
| 4 | emr_type | varchar(32) | 视图输出字段，语义来自底层查询定义（推断） |
| 5 | template_scope | varchar(16) | 视图输出字段，语义来自底层查询定义（推断） |
| 6 | template_structure | jsonb | 聚合 JSON 结构字段（推断） |
| 7 | enabled | boolean | 视图输出字段，语义来自底层查询定义（推断） |
| 8 | create_time | timestamp without time zone | 时间审计/业务时点字段（推断） |
| 9 | update_time | timestamp without time zone | 时间审计/业务时点字段（推断） |
| 10 | selected_dept_id | bigint | 关联标识字段（推断） |

**定义 SQL（数据库当前定义）**

```sql
 SELECT t.id,
    t.template_code,
    t.template_name,
    t.emr_type,
    t.template_scope,
    t.template_structure,
    t.enabled,
    t.create_time,
    t.update_time,
    td.dept_id AS selected_dept_id
   FROM (emr_template t
     LEFT JOIN emr_template_department td ON ((td.template_id = t.id)));
```


### 3.2 视图：v_emr_template_scope

**字段结构**

| 序号 | 字段名 | 类型 | 字段功能说明 |
|---:|---|---|---|
| 1 | id | bigint | 记录标识字段（推断） |
| 2 | template_code | varchar(64) | 业务编码字段（推断） |
| 3 | template_name | varchar(128) | 名称展示字段（推断） |
| 4 | emr_type | varchar(32) | 视图输出字段，语义来自底层查询定义（推断） |
| 5 | template_scope | varchar(16) | 视图输出字段，语义来自底层查询定义（推断） |
| 6 | scope_label | varchar() | 视图输出字段，语义来自底层查询定义（推断） |
| 7 | template_structure | jsonb | 聚合 JSON 结构字段（推断） |
| 8 | enabled | boolean | 视图输出字段，语义来自底层查询定义（推断） |
| 9 | create_time | timestamp without time zone | 时间审计/业务时点字段（推断） |
| 10 | update_time | timestamp without time zone | 时间审计/业务时点字段（推断） |
| 11 | dept_id | bigint | 关联标识字段（推断） |
| 12 | dept_code | varchar(64) | 业务编码字段（推断） |
| 13 | dept_name | varchar(128) | 名称展示字段（推断） |

**定义 SQL（数据库当前定义）**

```sql
 SELECT t.id,
    t.template_code,
    t.template_name,
    t.emr_type,
    t.template_scope,
        CASE
            WHEN ((t.template_scope)::text = 'HOSPITAL'::text) THEN '全院'::character varying
            WHEN ((t.template_scope)::text = 'DEPARTMENT'::text) THEN '科室'::character varying
            ELSE t.template_scope
        END AS scope_label,
    t.template_structure,
    t.enabled,
    t.create_time,
    t.update_time,
    td.dept_id,
    d.dept_code,
    d.dept_name
   FROM ((emr_template t
     LEFT JOIN emr_template_department td ON ((td.template_id = t.id)))
     LEFT JOIN sys_department d ON ((d.id = td.dept_id)));
```


### 3.2 视图：v_outpatient_emr_bundle

**字段结构**

| 序号 | 字段名 | 类型 | 字段功能说明 |
|---:|---|---|---|
| 1 | id | bigint | 记录标识字段（推断） |
| 2 | encounter_id | bigint | 关联标识字段（推断） |
| 3 | visit_code | varchar(64) | 业务编码字段（推断） |
| 4 | encounter_json | jsonb | 聚合 JSON 结构字段（推断） |
| 5 | emr_code | varchar(64) | 业务编码字段（推断） |
| 6 | patient_code | varchar(64) | 业务编码字段（推断） |
| 7 | doctor_code | varchar(64) | 业务编码字段（推断） |
| 8 | emr_type | varchar(32) | 视图输出字段，语义来自底层查询定义（推断） |
| 9 | emr_status | varchar(32) | 视图输出字段，语义来自底层查询定义（推断） |
| 10 | current_version | integer | 视图输出字段，语义来自底层查询定义（推断） |
| 11 | emr_content | jsonb | 聚合 JSON 结构字段（推断） |
| 12 | signed_flag | boolean | 视图输出字段，语义来自底层查询定义（推断） |
| 13 | sign_action | boolean | 视图输出字段，语义来自底层查询定义（推断） |
| 14 | template_code | varchar(64) | 业务编码字段（推断） |

**定义 SQL（数据库当前定义）**

```sql
 SELECT oed.id,
    oed.id AS encounter_id,
    oed.visit_code,
    oed.encounter_json,
    er.emr_code,
    er.patient_code,
    er.doctor_code,
    er.emr_type,
    er.emr_status,
    er.current_version,
    rv.emr_content,
    rv.signed_flag,
    false AS sign_action,
    er.template_code
   FROM ((outpatient_encounter_data oed
     LEFT JOIN emr_record er ON ((((er.visit_code)::text = (oed.visit_code)::text) AND ((er.emr_type)::text = 'OUTPATIENT'::text))))
     LEFT JOIN emr_record_version rv ON ((((rv.emr_code)::text = (er.emr_code)::text) AND (rv.version_no = er.current_version))));
```


### 3.2 视图：v_outpatient_emr_bundle_latest

**字段结构**

| 序号 | 字段名 | 类型 | 字段功能说明 |
|---:|---|---|---|
| 1 | id | bigint | 记录标识字段（推断） |
| 2 | encounter_id | bigint | 关联标识字段（推断） |
| 3 | visit_code | varchar(64) | 业务编码字段（推断） |
| 4 | encounter_json | jsonb | 聚合 JSON 结构字段（推断） |
| 5 | emr_record_id | bigint | 关联标识字段（推断） |
| 6 | emr_code | varchar(64) | 业务编码字段（推断） |
| 7 | patient_code | varchar(64) | 业务编码字段（推断） |
| 8 | doctor_code | varchar(64) | 业务编码字段（推断） |
| 9 | emr_type | varchar(32) | 视图输出字段，语义来自底层查询定义（推断） |
| 10 | emr_status | varchar(32) | 视图输出字段，语义来自底层查询定义（推断） |
| 11 | current_version | integer | 视图输出字段，语义来自底层查询定义（推断） |
| 12 | version_id | bigint | 关联标识字段（推断） |
| 13 | emr_content | jsonb | 聚合 JSON 结构字段（推断） |
| 14 | signed_flag | boolean | 视图输出字段，语义来自底层查询定义（推断） |
| 15 | version_no | integer | 视图输出字段，语义来自底层查询定义（推断） |
| 16 | update_time | timestamp without time zone | 时间审计/业务时点字段（推断） |

**定义 SQL（数据库当前定义）**

```sql
 SELECT oed.id,
    oed.id AS encounter_id,
    oed.visit_code,
    oed.encounter_json,
    er.id AS emr_record_id,
    er.emr_code,
    er.patient_code,
    er.doctor_code,
    er.emr_type,
    er.emr_status,
    er.current_version,
    rv.id AS version_id,
    rv.emr_content,
    rv.signed_flag,
    rv.version_no,
    GREATEST(COALESCE(oed.update_time, '1970-01-01 00:00:00'::timestamp without time zone), COALESCE(er.update_time, '1970-01-01 00:00:00'::timestamp without time zone), COALESCE(rv.update_time, '1970-01-01 00:00:00'::timestamp without time zone)) AS update_time
   FROM ((outpatient_encounter_data oed
     LEFT JOIN emr_record er ON ((((er.visit_code)::text = (oed.visit_code)::text) AND ((er.emr_type)::text = 'OUTPATIENT'::text))))
     LEFT JOIN emr_record_version rv ON ((((rv.emr_code)::text = (er.emr_code)::text) AND (rv.version_no = er.current_version))));
```


### 3.2 视图：v_outpatient_visit_detail

**字段结构**

| 序号 | 字段名 | 类型 | 字段功能说明 |
|---:|---|---|---|
| 1 | visit_code | varchar(64) | 业务编码字段（推断） |
| 2 | reg_code | varchar(64) | 业务编码字段（推断） |
| 3 | visit_type | varchar(32) | 视图输出字段，语义来自底层查询定义（推断） |
| 4 | visit_status | visit_status_enum | 视图输出字段，语义来自底层查询定义（推断） |
| 5 | start_time | timestamp without time zone | 时间审计/业务时点字段（推断） |
| 6 | end_time | timestamp without time zone | 时间审计/业务时点字段（推断） |
| 7 | dept_code | varchar(64) | 业务编码字段（推断） |
| 8 | doctor_code | varchar(64) | 业务编码字段（推断） |
| 9 | patient_code | varchar(64) | 业务编码字段（推断） |
| 10 | patient_name | varchar(64) | 名称展示字段（推断） |
| 11 | gender_code | varchar(32) | 业务编码字段（推断） |
| 12 | birthday | integer | 视图输出字段，语义来自底层查询定义（推断） |
| 13 | phone | varchar(20) | 视图输出字段，语义来自底层查询定义（推断） |
| 14 | id_card | varchar(18) | 视图输出字段，语义来自底层查询定义（推断） |
| 15 | doctor_name | varchar(128) | 名称展示字段（推断） |
| 16 | dept_name | varchar(128) | 名称展示字段（推断） |
| 17 | encounter_json | jsonb | 聚合 JSON 结构字段（推断） |
| 18 | emr_status | varchar(32) | 视图输出字段，语义来自底层查询定义（推断） |
| 19 | current_version | integer | 视图输出字段，语义来自底层查询定义（推断） |
| 20 | orders_json | jsonb | 聚合 JSON 结构字段（推断） |

**定义 SQL（数据库当前定义）**

```sql
 SELECT v.visit_code,
    v.reg_code,
    v.visit_type,
    v.visit_status,
    v.start_time,
    v.end_time,
    v.dept_code,
    v.doctor_code,
    p.patient_code,
    p.patient_name,
    p.gender_code,
    p.birthday,
    p.phone,
    p.id_card,
    d.doctor_name,
    dept.dept_name,
    oed.encounter_json,
    er.emr_status,
    er.current_version,
    ( SELECT COALESCE(jsonb_agg(x.* ORDER BY x.create_time), '[]'::jsonb) AS "coalesce"
           FROM ( SELECT mo.order_code,
                    mo.order_type,
                    mo.order_status,
                    mo.create_time,
                    mo.update_time,
                        CASE
                            WHEN (mo.order_type = 'DRUG'::order_type_enum) THEN ( SELECT COALESCE(jsonb_agg(jsonb_build_object('item_no', di.item_no, 'drug_name_snapshot', di.drug_name_snapshot, 'spec_snapshot', di.spec_snapshot, 'qty_value', di.qty_value, 'qty_unit_code', di.qty_unit_code, 'frequency_code', di.frequency_code, 'route_code', di.route_code, 'duration_days', di.duration_days) ORDER BY di.item_no), '[]'::jsonb) AS "coalesce"
                               FROM medical_order_drug_item di
                              WHERE ((di.order_code)::text = (mo.order_code)::text))
                            WHEN (mo.order_type = 'EXAM'::order_type_enum) THEN ( SELECT to_jsonb(me.*) AS to_jsonb
                               FROM medical_order_exam me
                              WHERE ((me.order_code)::text = (mo.order_code)::text)
                             LIMIT 1)
                            WHEN (mo.order_type = 'LAB'::order_type_enum) THEN ( SELECT COALESCE(jsonb_agg(jsonb_build_object('item_no', li.item_no, 'lab_item_code', li.lab_item_code, 'lab_item_name_snapshot', li.lab_item_name_snapshot, 'specimen_type', li.specimen_type, 'ref_low', li.ref_low, 'ref_high', li.ref_high, 'ref_text', li.ref_text, 'unit', li.unit) ORDER BY li.item_no), '[]'::jsonb) AS "coalesce"
                               FROM medical_order_lab_item li
                              WHERE ((li.order_code)::text = (mo.order_code)::text))
                            ELSE NULL::jsonb
                        END AS detail,
                    ( SELECT jsonb_build_object('report_status', ex.report_status, 'report_jsonb', ex.report_jsonb, 'report_xml', ex.report_xml, 'image_files', ex.image_files, 'final_diagnosis_jsonb', ex.final_diagnosis_jsonb, 'report_time', ex.report_time) AS jsonb_build_object
                           FROM exam_result ex
                          WHERE ((ex.order_code)::text = (mo.order_code)::text)
                         LIMIT 1) AS exam_result,
                    ( SELECT jsonb_build_object('report_status', lr.report_status, 'result_json', lr.result_json, 'report_time', lr.report_time, 'items', COALESCE(( SELECT jsonb_agg(jsonb_build_object('lab_item_code', lri.lab_item_code, 'item_name', lri.item_name, 'result_value_num', lri.result_value_num, 'result_value_text', lri.result_value_text, 'unit', lri.unit, 'ref_low', lri.ref_low, 'ref_high', lri.ref_high, 'ref_text', lri.ref_text, 'result_flag', lri.result_flag) ORDER BY lri.id) AS jsonb_agg
                                   FROM lab_result_item lri
                                  WHERE ((lri.order_code)::text = (mo.order_code)::text)), '[]'::jsonb)) AS jsonb_build_object
                           FROM lab_result lr
                          WHERE ((lr.order_code)::text = (mo.order_code)::text)
                         LIMIT 1) AS lab_result
                   FROM medical_order mo
                  WHERE ((mo.visit_code)::text = (v.visit_code)::text)) x) AS orders_json
   FROM (((((outpatient_visit_active v
     LEFT JOIN patient p ON (((p.patient_code)::text = (v.patient_code)::text)))
     LEFT JOIN doctor d ON (((d.doctor_code)::text = (v.doctor_code)::text)))
     LEFT JOIN sys_department dept ON (((dept.dept_code)::text = (v.dept_code)::text)))
     LEFT JOIN outpatient_encounter_data oed ON (((oed.visit_code)::text = (v.visit_code)::text)))
     LEFT JOIN emr_record er ON ((((er.visit_code)::text = (v.visit_code)::text) AND ((er.emr_type)::text = 'OUTPATIENT'::text))));
```

## 4. 触发器与触发器函数

### 4.1 触发器总览

| 对象 | 对象类型 | 触发器名 | 触发定义 | 触发函数 |
|---|---|---|---|---|
| emr_record | TABLE | trg_emr_record_archive | CREATE TRIGGER trg_emr_record_archive AFTER UPDATE OF emr_status ON emr_record FOR EACH ROW EXECUTE FUNCTION fn_emr_record_archive() | fn_emr_record_archive |
| emr_record | TABLE | trg_emr_record_guard_bu | CREATE TRIGGER trg_emr_record_guard_bu BEFORE UPDATE ON emr_record FOR EACH ROW EXECUTE FUNCTION fn_emr_record_guard_bu() | fn_emr_record_guard_bu |
| emr_record_history | TABLE | trg_emr_record_history_readonly | CREATE TRIGGER trg_emr_record_history_readonly BEFORE DELETE OR UPDATE ON emr_record_history FOR EACH ROW EXECUTE FUNCTION fn_emr_record_history_readonly() | fn_emr_record_history_readonly |
| emr_record_version | TABLE | trg_emr_record_version_guard_bd | CREATE TRIGGER trg_emr_record_version_guard_bd BEFORE DELETE ON emr_record_version FOR EACH ROW EXECUTE FUNCTION fn_emr_record_version_guard_bud() | fn_emr_record_version_guard_bud |
| emr_record_version | TABLE | trg_emr_record_version_guard_bi | CREATE TRIGGER trg_emr_record_version_guard_bi BEFORE INSERT ON emr_record_version FOR EACH ROW EXECUTE FUNCTION fn_emr_record_version_guard_bi() | fn_emr_record_version_guard_bi |
| emr_record_version | TABLE | trg_emr_record_version_guard_bu | CREATE TRIGGER trg_emr_record_version_guard_bu BEFORE UPDATE ON emr_record_version FOR EACH ROW EXECUTE FUNCTION fn_emr_record_version_guard_bud() | fn_emr_record_version_guard_bud |
| lab_result_item | TABLE | trg_lab_result_item_calc_flag_biu | CREATE TRIGGER trg_lab_result_item_calc_flag_biu BEFORE INSERT OR UPDATE ON lab_result_item FOR EACH ROW EXECUTE FUNCTION fn_lab_result_item_calc_flag_biu() | fn_lab_result_item_calc_flag_biu |
| medical_order | TABLE | trg_exam_order_complete_guard_bu | CREATE TRIGGER trg_exam_order_complete_guard_bu BEFORE UPDATE ON medical_order FOR EACH ROW EXECUTE FUNCTION fn_exam_order_complete_guard_bu() | fn_exam_order_complete_guard_bu |
| medical_order | TABLE | trg_medical_order_status_guard_bu | CREATE TRIGGER trg_medical_order_status_guard_bu BEFORE UPDATE ON medical_order FOR EACH ROW EXECUTE FUNCTION fn_medical_order_status_guard_bu() | fn_medical_order_status_guard_bu |
| medical_order_exam | TABLE | trg_medical_order_exam_item_valid | CREATE TRIGGER trg_medical_order_exam_item_valid BEFORE INSERT OR UPDATE ON medical_order_exam FOR EACH ROW EXECUTE FUNCTION fn_check_medical_order_exam_item_valid() | fn_check_medical_order_exam_item_valid |
| medical_order_exam_part | TABLE | trg_medical_order_exam_part_valid | CREATE TRIGGER trg_medical_order_exam_part_valid BEFORE INSERT OR UPDATE ON medical_order_exam_part FOR EACH ROW EXECUTE FUNCTION fn_check_medical_order_exam_part_valid() | fn_check_medical_order_exam_part_valid |
| outpatient_encounter_data | TABLE | trg_outpatient_encounter_guard_bu | CREATE TRIGGER trg_outpatient_encounter_guard_bu BEFORE UPDATE ON outpatient_encounter_data FOR EACH ROW EXECUTE FUNCTION fn_outpatient_encounter_guard_bu() | fn_outpatient_encounter_guard_bu |
| outpatient_visit_active | TABLE | trg_active_visit_create_encounter | CREATE TRIGGER trg_active_visit_create_encounter AFTER INSERT ON outpatient_visit_active FOR EACH ROW EXECUTE FUNCTION trg_active_visit_create_encounter() | trg_active_visit_create_encounter |
| outpatient_visit_active | TABLE | trg_active_visit_finalize | CREATE TRIGGER trg_active_visit_finalize AFTER UPDATE OF visit_status ON outpatient_visit_active FOR EACH ROW EXECUTE FUNCTION trg_active_visit_finalize() | trg_active_visit_finalize |
| outpatient_visit_active | TABLE | trg_active_visit_status_validate | CREATE TRIGGER trg_active_visit_status_validate BEFORE UPDATE OF visit_status ON outpatient_visit_active FOR EACH ROW EXECUTE FUNCTION trg_active_visit_status_validate() | trg_active_visit_status_validate |
| outpatient_visit_history | TABLE | trg_visit_history_readonly | CREATE TRIGGER trg_visit_history_readonly BEFORE DELETE OR UPDATE ON outpatient_visit_history FOR EACH ROW EXECUTE FUNCTION trg_visit_history_readonly() | trg_visit_history_readonly |
| registration | TABLE | trg_registration_cancel_to_visit | CREATE TRIGGER trg_registration_cancel_to_visit AFTER UPDATE OF reg_status ON registration FOR EACH ROW EXECUTE FUNCTION trg_registration_cancel_to_visit() | trg_registration_cancel_to_visit |
| registration | TABLE | trg_registration_create_active_visit | CREATE TRIGGER trg_registration_create_active_visit AFTER INSERT ON registration FOR EACH ROW EXECUTE FUNCTION trg_registration_create_active_visit() | trg_registration_create_active_visit |
| registration | TABLE | trg_registration_fill_reg_code | CREATE TRIGGER trg_registration_fill_reg_code BEFORE INSERT ON registration FOR EACH ROW EXECUTE FUNCTION trg_registration_fill_reg_code() | trg_registration_fill_reg_code |
| registration | TABLE | trg_registration_status_validate | CREATE TRIGGER trg_registration_status_validate BEFORE INSERT OR UPDATE OF reg_status ON registration FOR EACH ROW EXECUTE FUNCTION trg_registration_status_validate() | trg_registration_status_validate |
| v_doctor_user_manage | VIEW | trg_v_doctor_user_manage_iud | CREATE TRIGGER trg_v_doctor_user_manage_iud INSTEAD OF INSERT OR DELETE OR UPDATE ON v_doctor_user_manage FOR EACH ROW EXECUTE FUNCTION fn_v_doctor_user_manage_iud() | fn_v_doctor_user_manage_iud |
| v_emr_template_manage | VIEW | trg_v_emr_template_manage_iou | CREATE TRIGGER trg_v_emr_template_manage_iou INSTEAD OF INSERT OR UPDATE ON v_emr_template_manage FOR EACH ROW EXECUTE FUNCTION fn_v_emr_template_manage_iou() | fn_v_emr_template_manage_iou |
| v_outpatient_emr_bundle | VIEW | trg_v_outpatient_emr_bundle_u | CREATE TRIGGER trg_v_outpatient_emr_bundle_u INSTEAD OF UPDATE ON v_outpatient_emr_bundle FOR EACH ROW EXECUTE FUNCTION fn_v_outpatient_emr_bundle_u() | fn_v_outpatient_emr_bundle_u |

### 4.2 触发器函数清单与功能说明

说明：以下函数均来自数据库实时 `pg_get_functiondef`；功能说明基于源码逻辑提炼。

#### 函数：fn_check_medical_order_exam_item_valid

- 功能：校验检查医嘱项目合法性；回填项目快照、类型、增强/部位能力，防止非法项目落库。（来源：触发器函数源码 + 语义提炼）

```sql
CREATE OR REPLACE FUNCTION public.fn_check_medical_order_exam_item_valid()
 RETURNS trigger
 LANGUAGE plpgsql
AS $function$
DECLARE
  v_item RECORD;
BEGIN
  SELECT
    id,
    item_code,
    item_name,
    type_code,
    supports_part,
    supports_enhanced,
    status
  INTO v_item
  FROM public.emr_exam_item_dict
  WHERE item_code = NEW.exam_item_code;

  IF v_item.id IS NULL THEN
    RAISE EXCEPTION 'exam item % not found in emr_exam_item_dict', NEW.exam_item_code;
  END IF;

  IF COALESCE(v_item.status, 0) <> 1 THEN
    RAISE EXCEPTION 'exam item % is disabled', NEW.exam_item_code;
  END IF;

  IF COALESCE(NEW.is_enhanced, FALSE) = TRUE AND COALESCE(v_item.supports_enhanced, FALSE) = FALSE THEN
    RAISE EXCEPTION 'exam item % does not support enhanced mode', NEW.exam_item_code;
  END IF;

  IF COALESCE(v_item.supports_part, FALSE) = TRUE THEN
    IF NOT EXISTS (
      SELECT 1
      FROM public.emr_exam_item_part_map m
      WHERE m.item_code = NEW.exam_item_code
        AND m.status = 1
    ) THEN
      RAISE EXCEPTION 'exam item % has no active body-part mapping', NEW.exam_item_code;
    END IF;
  ELSE
    IF NEW.id IS NOT NULL AND EXISTS (
      SELECT 1
      FROM public.medical_order_exam_part p
      WHERE p.order_exam_id = NEW.id
    ) THEN
      RAISE EXCEPTION 'exam item % does not support body part but parts already exist', NEW.exam_item_code;
    END IF;
  END IF;

  NEW.exam_item_id := v_item.id;
  NEW.exam_type_code := v_item.type_code;
  NEW.supports_part := v_item.supports_part;
  NEW.supports_enhanced := v_item.supports_enhanced;
  NEW.exam_item_snapshot := v_item.item_name;
  NEW.exam_item_name := v_item.item_name;

  IF COALESCE(v_item.supports_part, FALSE) = FALSE THEN
    NEW.exam_part := NULL;
  END IF;

  RETURN NEW;
END;
$function$

```

#### 函数：fn_check_medical_order_exam_part_valid

- 功能：校验检查医嘱部位是否合法且与项目匹配。（来源：触发器函数源码 + 语义提炼）

```sql
CREATE OR REPLACE FUNCTION public.fn_check_medical_order_exam_part_valid()
 RETURNS trigger
 LANGUAGE plpgsql
AS $function$
DECLARE
  v_item_code VARCHAR(64);
  v_supports_part BOOLEAN;
BEGIN
  SELECT exam_item_code INTO v_item_code
  FROM public.medical_order_exam
  WHERE id = NEW.order_exam_id;

  IF v_item_code IS NULL THEN
    RAISE EXCEPTION 'order_exam_id=% not found or missing exam_item_code', NEW.order_exam_id;
  END IF;

  SELECT supports_part INTO v_supports_part
  FROM public.emr_exam_item_dict
  WHERE item_code = v_item_code;

  IF COALESCE(v_supports_part, FALSE) = FALSE THEN
    RAISE EXCEPTION 'exam item % does not support body part selection', v_item_code;
  END IF;

  IF NOT EXISTS (
    SELECT 1
    FROM public.emr_exam_item_part_map m
    WHERE m.item_code = v_item_code
      AND m.part_code = NEW.part_code
      AND m.status = 1
  ) THEN
    RAISE EXCEPTION 'part % is not allowed for exam item %', NEW.part_code, v_item_code;
  END IF;

  RETURN NEW;
END;
$function$

```

#### 函数：fn_emr_record_archive

- 功能：病历签署后归档当前版本内容到历史表，并保留就诊快照。（来源：触发器函数源码 + 语义提炼）

```sql
CREATE OR REPLACE FUNCTION public.fn_emr_record_archive()
 RETURNS trigger
 LANGUAGE plpgsql
AS $function$
DECLARE
  v_emr_content   jsonb;
  v_encounter_json jsonb;
  v_version_no    integer;
BEGIN
  -- 只处理 emr_status 变为 SIGNED 的更新
  IF TG_OP <> 'UPDATE'
     OR NEW.emr_status IS NOT DISTINCT FROM OLD.emr_status
     OR NEW.emr_status <> 'SIGNED'
  THEN
    RETURN NEW;
  END IF;

  -- 读取当前版本的病历内容
  v_version_no := COALESCE(NEW.current_version, 1);

  SELECT emr_content INTO v_emr_content
  FROM public.emr_record_version
  WHERE emr_code   = NEW.emr_code
    AND version_no = v_version_no
  ORDER BY id DESC
  LIMIT 1;

  -- 读取结构化就诊数据
  SELECT encounter_json INTO v_encounter_json
  FROM public.outpatient_encounter_data
  WHERE visit_code = NEW.visit_code
  LIMIT 1;

  -- 插入归档记录（冲突则更新，以支持重复签署场景）
  INSERT INTO public.emr_record_history (
    emr_code, visit_code, patient_code, doctor_code, template_code,
    emr_type, version_no, emr_content, encounter_json,
    signed_flag, signed_at, emr_status, create_time, update_time, archived_at
  ) VALUES (
    NEW.emr_code, NEW.visit_code, NEW.patient_code, NEW.doctor_code, NEW.template_code,
    NEW.emr_type, v_version_no,
    COALESCE(v_emr_content, '{}'::jsonb),
    COALESCE(v_encounter_json, '{}'::jsonb),
    true, CURRENT_TIMESTAMP, 'SIGNED',
    NEW.create_time, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP
  )
  ON CONFLICT (emr_code, version_no) DO UPDATE SET
    emr_content    = EXCLUDED.emr_content,
    encounter_json = EXCLUDED.encounter_json,
    signed_at      = EXCLUDED.signed_at,
    archived_at    = EXCLUDED.archived_at,
    update_time    = EXCLUDED.update_time;

  RETURN NEW;
END;
$function$

```

#### 函数：fn_emr_record_guard_bu

- 功能：病历更新前状态守卫，限制非法状态或关键字段变更。（来源：触发器函数源码 + 语义提炼）

```sql
CREATE OR REPLACE FUNCTION public.fn_emr_record_guard_bu()
 RETURNS trigger
 LANGUAGE plpgsql
AS $function$
BEGIN
  IF OLD.emr_status = 'SIGNED' THEN
    IF NEW IS DISTINCT FROM OLD THEN
      RAISE EXCEPTION 'emr_record 已签署，禁止修改（emr_code=%）', OLD.emr_code;
    END IF;
  END IF;

  IF COALESCE(OLD.emr_status, 'DRAFT') <> COALESCE(NEW.emr_status, 'DRAFT')
     AND NOT (OLD.emr_status = 'DRAFT' AND NEW.emr_status = 'SIGNED') THEN
    RAISE EXCEPTION '非法状态流转：% -> %（emr_code=%）', OLD.emr_status, NEW.emr_status, OLD.emr_code;
  END IF;

  RETURN NEW;
END;
$function$

```

#### 函数：fn_emr_record_history_readonly

- 功能：禁止病历历史表更新/删除，保证历史只增不可改。（来源：触发器函数源码 + 语义提炼）

```sql
CREATE OR REPLACE FUNCTION public.fn_emr_record_history_readonly()
 RETURNS trigger
 LANGUAGE plpgsql
AS $function$
BEGIN
  IF TG_OP IN ('UPDATE', 'DELETE') THEN
    RAISE EXCEPTION '电子病历归档记录不允许修改或删除（visit_code=%）', OLD.visit_code;
  END IF;
  RETURN NEW;
END;
$function$

```

#### 函数：fn_emr_record_version_guard_bi

- 功能：病历版本插入前守卫，保证版本号与关联一致性。（来源：触发器函数源码 + 语义提炼）

```sql
CREATE OR REPLACE FUNCTION public.fn_emr_record_version_guard_bi()
 RETURNS trigger
 LANGUAGE plpgsql
AS $function$
DECLARE
  v_status varchar(32);
  v_current integer;
BEGIN
  SELECT emr_status, current_version
    INTO v_status, v_current
    FROM public.emr_record
   WHERE emr_code = NEW.emr_code;

  IF NOT FOUND THEN
    RAISE EXCEPTION 'emr_record_version 插入失败：emr_code=% 对应主记录不存在', NEW.emr_code;
  END IF;

  IF COALESCE(v_status, 'DRAFT') = 'SIGNED' AND COALESCE(NEW.signed_flag, false) = false THEN
    RAISE EXCEPTION 'emr_record 已签署时，禁止写入未签署版本（emr_code=%）', NEW.emr_code;
  END IF;

  IF COALESCE(NEW.signed_flag, false) = true THEN
    IF v_status <> 'SIGNED' THEN
      RAISE EXCEPTION 'signed_flag=true 时主记录必须为 SIGNED（emr_code=%）', NEW.emr_code;
    END IF;
    IF NEW.version_no <> v_current THEN
      RAISE EXCEPTION 'signed_flag=true 时 version_no 必须等于 current_version（emr_code=%）', NEW.emr_code;
    END IF;
  END IF;

  RETURN NEW;
END;
$function$

```

#### 函数：fn_emr_record_version_guard_bud

- 功能：病历版本更新/删除守卫，防止破坏既有版本链。（来源：触发器函数源码 + 语义提炼）

```sql
CREATE OR REPLACE FUNCTION public.fn_emr_record_version_guard_bud()
 RETURNS trigger
 LANGUAGE plpgsql
AS $function$
BEGIN
  IF TG_OP = 'UPDATE' THEN
    RAISE EXCEPTION 'emr_record_version 只允许新增，禁止更新（id=%）', OLD.id;
  ELSIF TG_OP = 'DELETE' THEN
    IF current_setting('app.allow_version_purge', true) = 'on' THEN
      RETURN OLD;
    END IF;
    RAISE EXCEPTION 'emr_record_version 只允许新增，禁止删除（id=%）', OLD.id;
  END IF;
  RETURN NULL;
END;
$function$

```

#### 函数：fn_exam_order_complete_guard_bu

- 功能：医嘱完成前置校验，保证检查/检验结果闭环条件。（来源：触发器函数源码 + 语义提炼）

```sql
CREATE OR REPLACE FUNCTION public.fn_exam_order_complete_guard_bu()
 RETURNS trigger
 LANGUAGE plpgsql
AS $function$
DECLARE
  v_report_status VARCHAR(16);
BEGIN
  IF NEW.order_type = 'EXAM' AND OLD.order_status <> 'COMPLETED' AND NEW.order_status = 'COMPLETED' THEN
    SELECT er.report_status INTO v_report_status
      FROM public.exam_result er
     WHERE er.order_code = NEW.order_code;

    IF COALESCE(v_report_status, '') <> 'FINAL' THEN
      RAISE EXCEPTION '检查医嘱完成失败：请先完成检查报告';
    END IF;
  END IF;

  RETURN NEW;
END;
$function$

```

#### 函数：fn_lab_result_item_calc_flag_biu

- 功能：检验明细写入时自动计算异常标志（高/低/正常等）。（来源：触发器函数源码 + 语义提炼）

```sql
CREATE OR REPLACE FUNCTION public.fn_lab_result_item_calc_flag_biu()
 RETURNS trigger
 LANGUAGE plpgsql
AS $function$
BEGIN
  NEW.result_flag := 'N';

  IF NEW.result_value_num IS NOT NULL THEN
    IF NEW.critical_high IS NOT NULL AND NEW.result_value_num > NEW.critical_high THEN
      NEW.result_flag := 'C';
    ELSIF NEW.critical_low IS NOT NULL AND NEW.result_value_num < NEW.critical_low THEN
      NEW.result_flag := 'C';
    ELSIF NEW.ref_high IS NOT NULL AND NEW.result_value_num > NEW.ref_high THEN
      NEW.result_flag := 'H';
    ELSIF NEW.ref_low IS NOT NULL AND NEW.result_value_num < NEW.ref_low THEN
      NEW.result_flag := 'L';
    END IF;
  END IF;

  NEW.update_time := NOW();
  RETURN NEW;
END;
$function$

```

#### 函数：fn_medical_order_status_guard_bu

- 功能：医嘱状态流转守卫，限制非法状态跃迁。（来源：触发器函数源码 + 语义提炼）

```sql
CREATE OR REPLACE FUNCTION public.fn_medical_order_status_guard_bu()
 RETURNS trigger
 LANGUAGE plpgsql
AS $function$
BEGIN
  IF NEW.order_status IS DISTINCT FROM OLD.order_status THEN
    IF OLD.order_status = 'PENDING_RECEIVE' AND NEW.order_status NOT IN ('RECEIVED','CANCELLED') THEN
      RAISE EXCEPTION '非法状态流转: % -> %', OLD.order_status, NEW.order_status;
    ELSIF OLD.order_status = 'RECEIVED' AND NEW.order_status NOT IN ('EXECUTING','COMPLETED') THEN
      RAISE EXCEPTION '非法状态流转: % -> %', OLD.order_status, NEW.order_status;
    ELSIF OLD.order_status = 'EXECUTING' AND NEW.order_status <> 'COMPLETED' THEN
      RAISE EXCEPTION '非法状态流转: % -> %', OLD.order_status, NEW.order_status;
    ELSIF OLD.order_status IN ('COMPLETED','CANCELLED') THEN
      RAISE EXCEPTION '终态医嘱不允许变更: % -> %', OLD.order_status, NEW.order_status;
    END IF;

    NEW.status_version := COALESCE(OLD.status_version, 0) + 1;
    NEW.update_time := NOW();

    IF NEW.order_status = 'RECEIVED' THEN
      NEW.received_time := COALESCE(NEW.received_time, NOW());
    ELSIF NEW.order_status = 'COMPLETED' THEN
      NEW.completed_time := COALESCE(NEW.completed_time, NOW());
    ELSIF NEW.order_status = 'CANCELLED' THEN
      NEW.cancelled_time := COALESCE(NEW.cancelled_time, NOW());
    END IF;
  END IF;

  RETURN NEW;
END;
$function$

```

#### 函数：fn_outpatient_encounter_guard_bu

- 功能：门诊就诊结构化数据更新守卫，保护关键关联。（来源：触发器函数源码 + 语义提炼）

```sql
CREATE OR REPLACE FUNCTION public.fn_outpatient_encounter_guard_bu()
 RETURNS trigger
 LANGUAGE plpgsql
AS $function$
DECLARE
  v_signed boolean;
BEGIN
  SELECT EXISTS (
    SELECT 1
      FROM public.emr_record er
     WHERE er.visit_code = OLD.visit_code
       AND er.emr_type = 'OUTPATIENT'
       AND er.emr_status = 'SIGNED'
  ) INTO v_signed;

  IF v_signed AND (NEW.encounter_json IS DISTINCT FROM OLD.encounter_json) THEN
    RAISE EXCEPTION '就诊数据禁止修改：对应病历已签署（visit_code=%）', OLD.visit_code;
  END IF;

  RETURN NEW;
END;
$function$

```

#### 函数：fn_v_doctor_user_manage_iud

- 功能：视图写操作转发到 doctor/sys_user，实现联动增改删。（来源：触发器函数源码 + 语义提炼）

```sql
CREATE OR REPLACE FUNCTION public.fn_v_doctor_user_manage_iud()
 RETURNS trigger
 LANGUAGE plpgsql
AS $function$
DECLARE
    v_username varchar(50);
    v_doctor_name varchar(128);
    v_dept_code varchar(64);
    v_title_code varchar(32);
    v_license_no varchar(64);
    v_password_hash varchar(100);
    v_rc integer;
BEGIN
    IF TG_OP = 'INSERT' THEN
        IF NEW.doctor_code IS NULL OR btrim(NEW.doctor_code) = '' THEN
            RAISE EXCEPTION 'doctor_code 不能为空';
        END IF;
        IF NEW.doctor_name IS NULL OR btrim(NEW.doctor_name) = '' THEN
            RAISE EXCEPTION 'doctor_name 不能为空';
        END IF;
        IF NEW.dept_code IS NULL OR btrim(NEW.dept_code) = '' THEN
            RAISE EXCEPTION 'dept_code 不能为空';
        END IF;
        IF NEW.username IS NULL OR btrim(NEW.username) = '' THEN
            RAISE EXCEPTION 'username 不能为空';
        END IF;
        IF NEW.password IS NULL OR btrim(NEW.password) = '' THEN
            RAISE EXCEPTION 'password 不能为空';
        END IF;
        IF length(btrim(NEW.password)) < 6 THEN
            RAISE EXCEPTION 'password 长度不能小于 6';
        END IF;
        IF NEW.user_status IS NOT NULL AND NEW.user_status NOT IN (0, 1) THEN
            RAISE EXCEPTION 'user_status 仅允许 0 或 1';
        END IF;

        v_username := btrim(NEW.username);
        v_doctor_name := btrim(NEW.doctor_name);
        v_dept_code := btrim(NEW.dept_code);
        v_title_code := NULLIF(btrim(COALESCE(NEW.title_code, '')), '');
        v_license_no := NULLIF(btrim(COALESCE(NEW.license_no, '')), '');
        v_password_hash := CASE
            WHEN NEW.password ~ '^\$2[aby]\$\d{2}\$[./A-Za-z0-9]{53}$' THEN NEW.password
            ELSE crypt(NEW.password, gen_salt('bf', 10))
        END;

        IF EXISTS (SELECT 1 FROM public.sys_user u WHERE u.username = v_username) THEN
            RAISE EXCEPTION 'username 已存在: %', v_username;
        END IF;
        IF EXISTS (SELECT 1 FROM public.doctor d WHERE d.doctor_code = btrim(NEW.doctor_code)) THEN
            RAISE EXCEPTION 'doctor_code 已存在: %', btrim(NEW.doctor_code);
        END IF;

        INSERT INTO public.sys_user (
            username,
            password,
            name,
            status,
            create_time,
            update_time,
            is_deleted
        )
        VALUES (
            v_username,
            v_password_hash,
            COALESCE(NULLIF(v_doctor_name, ''), v_username),
            COALESCE(NEW.user_status, 1),
            CURRENT_TIMESTAMP,
            CURRENT_TIMESTAMP,
            0
        );

        INSERT INTO public.doctor (
            doctor_code,
            doctor_name,
            dept_code,
            title_code,
            license_no,
            user_username,
            enabled,
            create_time,
            update_time
        )
        VALUES (
            btrim(NEW.doctor_code),
            v_doctor_name,
            v_dept_code,
            v_title_code,
            v_license_no,
            v_username,
            COALESCE(NEW.doctor_enabled, true),
            CURRENT_TIMESTAMP,
            CURRENT_TIMESTAMP
        );

        RETURN NEW;
    ELSIF TG_OP = 'UPDATE' THEN
        IF COALESCE(NEW.username, '') <> COALESCE(OLD.username, '') THEN
            RAISE EXCEPTION 'username 不允许修改';
        END IF;
        IF NEW.user_status IS NOT NULL AND NEW.user_status NOT IN (0, 1) THEN
            RAISE EXCEPTION 'user_status 仅允许 0 或 1';
        END IF;
        IF NEW.password IS NOT NULL
           AND btrim(NEW.password) <> ''
           AND length(btrim(NEW.password)) < 6 THEN
            RAISE EXCEPTION 'password 长度不能小于 6';
        END IF;

        UPDATE public.doctor d
        SET doctor_name = COALESCE(NULLIF(btrim(NEW.doctor_name), ''), d.doctor_name),
            dept_code = COALESCE(NULLIF(btrim(NEW.dept_code), ''), d.dept_code),
            title_code = CASE
                WHEN NEW.title_code IS NULL THEN d.title_code
                ELSE NULLIF(btrim(NEW.title_code), '')
            END,
            license_no = CASE
                WHEN NEW.license_no IS NULL THEN d.license_no
                ELSE NULLIF(btrim(NEW.license_no), '')
            END,
            enabled = COALESCE(NEW.doctor_enabled, d.enabled),
            update_time = CURRENT_TIMESTAMP
        WHERE d.id = OLD.doctor_id;
        GET DIAGNOSTICS v_rc = ROW_COUNT;
        IF v_rc = 0 THEN
            RAISE EXCEPTION 'doctor 记录不存在或已被并发修改, doctor_id=%', OLD.doctor_id;
        END IF;

        UPDATE public.sys_user u
        SET name = COALESCE(NULLIF(btrim(NEW.doctor_name), ''), u.name),
            status = COALESCE(NEW.user_status, u.status),
            password = CASE
                WHEN NEW.password IS NULL OR btrim(NEW.password) = '' THEN u.password
                WHEN NEW.password ~ '^\$2[aby]\$\d{2}\$[./A-Za-z0-9]{53}$' THEN NEW.password
                ELSE crypt(NEW.password, gen_salt('bf', 10))
            END,
            update_time = CURRENT_TIMESTAMP
        WHERE u.id = OLD.user_id;
        GET DIAGNOSTICS v_rc = ROW_COUNT;
        IF v_rc = 0 THEN
            RAISE EXCEPTION 'sys_user 记录不存在或已被并发修改, user_id=%', OLD.user_id;
        END IF;

        RETURN NEW;
    ELSIF TG_OP = 'DELETE' THEN
        UPDATE public.doctor
        SET enabled = false,
            update_time = CURRENT_TIMESTAMP
        WHERE id = OLD.doctor_id;
        GET DIAGNOSTICS v_rc = ROW_COUNT;
        IF v_rc = 0 THEN
            RAISE EXCEPTION 'doctor 记录不存在, doctor_id=%', OLD.doctor_id;
        END IF;

        UPDATE public.sys_user
        SET is_deleted = 1,
            update_time = CURRENT_TIMESTAMP
        WHERE id = OLD.user_id;
        GET DIAGNOSTICS v_rc = ROW_COUNT;
        IF v_rc = 0 THEN
            RAISE EXCEPTION 'sys_user 记录不存在, user_id=%', OLD.user_id;
        END IF;

        RETURN OLD;
    END IF;

    RETURN NULL;
END;
$function$

```

#### 函数：fn_v_emr_template_manage_iou

- 功能：模板管理视图写操作转发到 emr_template 与科室关联表。（来源：触发器函数源码 + 语义提炼）

```sql
CREATE OR REPLACE FUNCTION public.fn_v_emr_template_manage_iou()
 RETURNS trigger
 LANGUAGE plpgsql
AS $function$
DECLARE
  v_template_id BIGINT;
BEGIN
  IF TG_OP = 'INSERT' THEN
    IF NEW.template_scope = 'DEPARTMENT' AND NEW.selected_dept_id IS NULL THEN
      RAISE EXCEPTION '科室范围模板必须传入 selected_dept_id';
    END IF;

    INSERT INTO public.emr_template (
      template_code,
      template_name,
      emr_type,
      template_scope,
      template_structure,
      enabled,
      create_time,
      update_time
    )
    VALUES (
      NEW.template_code,
      NEW.template_name,
      NEW.emr_type,
      NEW.template_scope,
      COALESCE(NEW.template_structure, '{}'::jsonb),
      COALESCE(NEW.enabled, TRUE),
      COALESCE(NEW.create_time, NOW()),
      NOW()
    )
    RETURNING id INTO v_template_id;

    IF NEW.template_scope = 'DEPARTMENT' THEN
      INSERT INTO public.emr_template_department (
        template_id,
        dept_id,
        create_time,
        update_time
      )
      VALUES (
        v_template_id,
        NEW.selected_dept_id,
        NOW(),
        NOW()
      )
      ON CONFLICT (template_id, dept_id)
      DO UPDATE SET update_time = EXCLUDED.update_time;
    END IF;

    NEW.id := v_template_id;
    NEW.update_time := NOW();
    RETURN NEW;
  END IF;

  IF TG_OP = 'UPDATE' THEN
    IF COALESCE(NEW.id, OLD.id) IS NULL THEN
      RAISE EXCEPTION '更新模板时必须指定 id';
    END IF;

    IF NEW.template_scope = 'DEPARTMENT' AND NEW.selected_dept_id IS NULL THEN
      RAISE EXCEPTION '科室范围模板必须传入 selected_dept_id';
    END IF;

    v_template_id := COALESCE(NEW.id, OLD.id);

    UPDATE public.emr_template
    SET
      template_code = NEW.template_code,
      template_name = NEW.template_name,
      emr_type = NEW.emr_type,
      template_scope = NEW.template_scope,
      template_structure = COALESCE(NEW.template_structure, '{}'::jsonb),
      enabled = COALESCE(NEW.enabled, TRUE),
      update_time = NOW()
    WHERE id = v_template_id;

    IF NEW.template_scope = 'HOSPITAL' THEN
      DELETE FROM public.emr_template_department
      WHERE template_id = v_template_id;
    ELSE
      DELETE FROM public.emr_template_department
      WHERE template_id = v_template_id
        AND dept_id <> NEW.selected_dept_id;

      INSERT INTO public.emr_template_department (
        template_id,
        dept_id,
        create_time,
        update_time
      )
      VALUES (
        v_template_id,
        NEW.selected_dept_id,
        NOW(),
        NOW()
      )
      ON CONFLICT (template_id, dept_id)
      DO UPDATE SET update_time = EXCLUDED.update_time;
    END IF;

    NEW.id := v_template_id;
    NEW.update_time := NOW();
    RETURN NEW;
  END IF;

  RAISE EXCEPTION '不支持的触发动作: %', TG_OP;
END;
$function$

```

#### 函数：fn_v_outpatient_emr_bundle_u

- 功能：门诊病历打包视图更新转发，落地到就诊/病历/版本等底层对象。（来源：触发器函数源码 + 语义提炼）

```sql
CREATE OR REPLACE FUNCTION public.fn_v_outpatient_emr_bundle_u()
 RETURNS trigger
 LANGUAGE plpgsql
AS $function$
DECLARE
  v_encounter public.outpatient_encounter_data%ROWTYPE;
  v_record public.emr_record%ROWTYPE;
  v_record_exists boolean := false;
  v_emr_code varchar(64);
  v_next_version integer;
  v_now timestamp := NOW();
  v_sign_action boolean := COALESCE(NEW.sign_action, false) OR COALESCE(NEW.signed_flag, false);
BEGIN
  IF TG_OP <> 'UPDATE' THEN
    RAISE EXCEPTION 'v_outpatient_emr_bundle 仅支持 UPDATE';
  END IF;

  IF COALESCE(NEW.visit_code, OLD.visit_code) IS NULL THEN
    RAISE EXCEPTION '保存失败：visit_code 不能为空';
  END IF;

  SELECT *
    INTO v_encounter
    FROM public.outpatient_encounter_data
   WHERE visit_code = COALESCE(NEW.visit_code, OLD.visit_code)
   FOR UPDATE;

  IF NOT FOUND THEN
    RAISE EXCEPTION '保存失败：visit_code=% 未找到就诊数据，请先创建空 JSONB 记录', COALESCE(NEW.visit_code, OLD.visit_code);
  END IF;

  SELECT *
    INTO v_record
    FROM public.emr_record
   WHERE visit_code = v_encounter.visit_code
     AND emr_type = COALESCE(NULLIF(NEW.emr_type, ''), 'OUTPATIENT')
   FOR UPDATE;
  v_record_exists := FOUND;

  IF v_record_exists AND v_record.emr_status = 'SIGNED' THEN
    RAISE EXCEPTION '保存失败：病历已签署，禁止修改（visit_code=%）', v_encounter.visit_code;
  END IF;

  UPDATE public.outpatient_encounter_data
     SET encounter_json = COALESCE(NEW.encounter_json, '{}'::jsonb),
         update_time = v_now
   WHERE id = v_encounter.id;

  IF NOT v_record_exists THEN
    v_emr_code := COALESCE(NULLIF(NEW.emr_code, ''), 'EMR_' || v_encounter.visit_code || '_' || to_char(clock_timestamp(), 'YYYYMMDDHH24MISSMS'));
    v_next_version := 1;

    INSERT INTO public.emr_record (
      emr_code,
      visit_code,
      patient_code,
      doctor_code,
      template_code,
      emr_type,
      emr_status,
      current_version,
      create_time,
      update_time
    )
    VALUES (
      v_emr_code,
      v_encounter.visit_code,
      COALESCE(NULLIF(NEW.patient_code, ''), ''),
      COALESCE(NULLIF(NEW.doctor_code, ''), ''),
      COALESCE(NULLIF(NEW.template_code, ''), 'OUTPATIENT_RUNTIME'),
      COALESCE(NULLIF(NEW.emr_type, ''), 'OUTPATIENT'),
      CASE WHEN v_sign_action THEN 'SIGNED' ELSE 'DRAFT' END,
      v_next_version,
      v_now,
      v_now
    );
  ELSE
    v_emr_code := v_record.emr_code;
    v_next_version := COALESCE(v_record.current_version, 0) + 1;

    UPDATE public.emr_record
       SET current_version = v_next_version,
           emr_status = CASE WHEN v_sign_action THEN 'SIGNED' ELSE 'DRAFT' END,
           update_time = v_now
     WHERE id = v_record.id;
  END IF;

  INSERT INTO public.emr_record_version (
    emr_code,
    version_no,
    emr_content,
    signed_flag,
    create_time,
    update_time
  )
  VALUES (
    v_emr_code,
    v_next_version,
    COALESCE(NEW.emr_content, '{}'::jsonb),
    v_sign_action,
    v_now,
    v_now
  );

  IF v_sign_action THEN
    INSERT INTO public.emr_record_history (
      emr_code,
      visit_code,
      patient_code,
      doctor_code,
      template_code,
      emr_type,
      version_no,
      emr_content,
      encounter_json,
      signed_flag,
      signed_at,
      emr_status,
      create_time,
      update_time,
      archived_at
    )
    SELECT
      er.emr_code,
      er.visit_code,
      er.patient_code,
      er.doctor_code,
      er.template_code,
      er.emr_type,
      v_next_version,
      COALESCE(NEW.emr_content, '{}'::jsonb),
      COALESCE(NEW.encounter_json, '{}'::jsonb),
      true,
      v_now,
      'SIGNED',
      er.create_time,
      v_now,
      v_now
    FROM public.emr_record er
    WHERE er.emr_code = v_emr_code
    ON CONFLICT (emr_code, version_no) DO UPDATE SET
      emr_content = EXCLUDED.emr_content,
      encounter_json = EXCLUDED.encounter_json,
      signed_at = EXCLUDED.signed_at,
      archived_at = EXCLUDED.archived_at,
      update_time = EXCLUDED.update_time;
  END IF;

  IF v_sign_action THEN
    PERFORM set_config('app.allow_version_purge', 'on', true);
    DELETE FROM public.emr_record_version
     WHERE emr_code = v_emr_code
       AND version_no <> v_next_version
       AND COALESCE(signed_flag, false) = false;
  END IF;

  NEW.emr_code := v_emr_code;
  NEW.current_version := v_next_version;
  NEW.emr_status := CASE WHEN v_sign_action THEN 'SIGNED' ELSE 'DRAFT' END;
  NEW.signed_flag := v_sign_action;
  RETURN NEW;
END;
$function$

```

#### 函数：trg_active_visit_create_encounter

- 功能：门诊活动就诊创建后自动初始化 encounter 数据。（来源：触发器函数源码 + 语义提炼）

```sql
CREATE OR REPLACE FUNCTION public.trg_active_visit_create_encounter()
 RETURNS trigger
 LANGUAGE plpgsql
AS $function$
BEGIN
  INSERT INTO public.outpatient_encounter_data (visit_code, encounter_json, create_time, update_time)
  VALUES (NEW.visit_code, '{}'::jsonb, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP)
  ON CONFLICT (visit_code) DO NOTHING;
  RETURN NEW;
END;
$function$

```

#### 函数：trg_active_visit_finalize

- 功能：门诊活动就诊完成后归档至历史表并从 active 表移除。（来源：触发器函数源码 + 语义提炼）

```sql
CREATE OR REPLACE FUNCTION public.trg_active_visit_finalize()
 RETURNS trigger
 LANGUAGE plpgsql
AS $function$
DECLARE
  v_json         jsonb := '{}'::jsonb;
  v_signed_count integer;
BEGIN
  IF TG_OP = 'UPDATE' AND OLD.visit_status IS DISTINCT FROM NEW.visit_status THEN
    IF NEW.visit_status = 'COMPLETED' THEN

      -- 检查是否有已签署的病历归档记录
      SELECT COUNT(*) INTO v_signed_count
      FROM public.emr_record_history
      WHERE visit_code = NEW.visit_code
        AND signed_flag = true;

      -- 兼容：若归档表无记录，也检查 emr_record 的当前状态
      IF v_signed_count = 0 THEN
        SELECT COUNT(*) INTO v_signed_count
        FROM public.emr_record
        WHERE visit_code  = NEW.visit_code
          AND emr_status  = 'SIGNED';
      END IF;

      IF v_signed_count = 0 THEN
        RAISE EXCEPTION '就诊 % 尚无已签署的电子病历，请先签署病历再结算', NEW.visit_code;
      END IF;

      -- 获取就诊结构化数据存入历史
      SELECT encounter_json INTO v_json
      FROM public.outpatient_encounter_data
      WHERE visit_code = NEW.visit_code;

      INSERT INTO public.outpatient_visit_history (
        visit_code, reg_code, patient_code, dept_code, doctor_code,
        visit_type, visit_status, start_time, end_time, create_time, update_time,
        encounter_json, archived_at, archive_reason
      ) VALUES (
        NEW.visit_code, NEW.reg_code, NEW.patient_code, NEW.dept_code, NEW.doctor_code,
        NEW.visit_type, NEW.visit_status,
        NEW.start_time, COALESCE(NEW.end_time, CURRENT_TIMESTAMP), NEW.create_time, CURRENT_TIMESTAMP,
        COALESCE(v_json, '{}'::jsonb), CURRENT_TIMESTAMP, 'COMPLETED'
      )
      ON CONFLICT (visit_code) DO NOTHING;

      DELETE FROM public.outpatient_visit_active WHERE id = NEW.id;
    END IF;
  END IF;

  RETURN NEW;
END;
$function$

```

#### 函数：trg_active_visit_status_validate

- 功能：校验门诊就诊状态机仅允许 REGISTERED->IN_CONSULTATION->COMPLETED。（来源：触发器函数源码 + 语义提炼）

```sql
CREATE OR REPLACE FUNCTION public.trg_active_visit_status_validate()
 RETURNS trigger
 LANGUAGE plpgsql
AS $function$
BEGIN
  IF TG_OP = 'UPDATE' AND OLD.visit_status IS DISTINCT FROM NEW.visit_status THEN
    IF OLD.visit_status = 'REGISTERED' AND NEW.visit_status = 'IN_CONSULTATION' THEN
      RETURN NEW;
    ELSIF OLD.visit_status = 'IN_CONSULTATION' AND NEW.visit_status = 'COMPLETED' THEN
      RETURN NEW;
    ELSE
      RAISE EXCEPTION '非法状态流转: % -> %', OLD.visit_status, NEW.visit_status;
    END IF;
  END IF;

  RETURN NEW;
END;
$function$

```

#### 函数：trg_registration_cancel_to_visit

- 功能：挂号取消（未就诊）时联动删除对应 active 就诊记录。（来源：触发器函数源码 + 语义提炼）

```sql
CREATE OR REPLACE FUNCTION public.trg_registration_cancel_to_visit()
 RETURNS trigger
 LANGUAGE plpgsql
AS $function$
DECLARE
  v_status public.visit_status_enum;
BEGIN
  IF TG_OP = 'UPDATE'
     AND NEW.reg_status IS DISTINCT FROM OLD.reg_status
     AND NEW.reg_status = 'CANCELLED_UNSEEN' THEN

    SELECT visit_status INTO v_status
    FROM public.outpatient_visit_active
    WHERE reg_code = NEW.reg_code
    FOR UPDATE;

    IF v_status IS NULL THEN
      RETURN NEW;
    END IF;

    IF v_status <> 'REGISTERED' THEN
      RAISE EXCEPTION '当前就诊已进入流程(%)，不允许执行未就诊取消', v_status;
    END IF;

    DELETE FROM public.outpatient_visit_active WHERE reg_code = NEW.reg_code;
  END IF;

  RETURN NEW;
END;
$function$

```

#### 函数：trg_registration_create_active_visit

- 功能：挂号成功后自动创建 active 就诊记录。（来源：触发器函数源码 + 语义提炼）

```sql
CREATE OR REPLACE FUNCTION public.trg_registration_create_active_visit()
 RETURNS trigger
 LANGUAGE plpgsql
AS $function$
BEGIN
  IF NEW.reg_status = 'REGISTERED' THEN
    INSERT INTO public.outpatient_visit_active (
      visit_code, reg_code, patient_code, dept_code, doctor_code,
      visit_type, visit_status, start_time, end_time, create_time, update_time
    ) VALUES (
      public.gen_visit_code(), NEW.reg_code, NEW.patient_code, NEW.dept_code, NEW.doctor_code,
      'FIRST', 'REGISTERED', CURRENT_TIMESTAMP, NULL, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP
    )
    ON CONFLICT (reg_code) DO NOTHING;
  END IF;

  RETURN NEW;
END;
$function$

```

#### 函数：trg_registration_fill_reg_code

- 功能：挂号插入前自动补齐 reg_code。（来源：触发器函数源码 + 语义提炼）

```sql
CREATE OR REPLACE FUNCTION public.trg_registration_fill_reg_code()
 RETURNS trigger
 LANGUAGE plpgsql
AS $function$
BEGIN
  IF NEW.reg_code IS NULL OR btrim(NEW.reg_code) = '' THEN
    NEW.reg_code := public.gen_reg_code();
  END IF;
  RETURN NEW;
END;
$function$

```

#### 函数：trg_registration_status_validate

- 功能：校验挂号状态合法性并限制不可逆恢复。（来源：触发器函数源码 + 语义提炼）

```sql
CREATE OR REPLACE FUNCTION public.trg_registration_status_validate()
 RETURNS trigger
 LANGUAGE plpgsql
AS $function$
BEGIN
  IF NEW.reg_status IS NULL OR btrim(NEW.reg_status) = '' THEN
    NEW.reg_status := 'REGISTERED';
  END IF;

  IF NEW.reg_status NOT IN ('REGISTERED', 'CANCELLED_UNSEEN') THEN
    RAISE EXCEPTION 'registration.reg_status 非法值: %', NEW.reg_status;
  END IF;

  IF TG_OP = 'UPDATE' AND OLD.reg_status = 'CANCELLED_UNSEEN' AND NEW.reg_status = 'REGISTERED' THEN
    RAISE EXCEPTION '已取消挂号不可恢复为已挂号';
  END IF;

  RETURN NEW;
END;
$function$

```

#### 函数：trg_visit_history_readonly

- 功能：限制 outpatient_visit_history 仅允许 INSERT。（来源：触发器函数源码 + 语义提炼）

```sql
CREATE OR REPLACE FUNCTION public.trg_visit_history_readonly()
 RETURNS trigger
 LANGUAGE plpgsql
AS $function$
BEGIN
  RAISE EXCEPTION 'outpatient_visit_history 仅允许INSERT，不允许%操作', TG_OP;
END;
$function$

```

## 5. 系统配置表附录

以下表按你的要求不纳入主数据/业务数据两大模块，单独作为系统配置与权限附录。

- APIJSON 配置表：`Access`、`Request`、`Function`、`Script`、`Document`
- 系统管理表：`sys_department`、`sys_user`、`sys_role`、`sys_menu`、`sys_user_role`、`sys_role_menu`、`sys_login_log`、`sys_oper_log`

### 5.1 系统配置表字段详表

### Access

- 表功能：业务/配置表（推断：库中未提供表注释）
- 表注释：（无）
- 字段数：12

**字段结构与功能**

| 序号 | 字段名 | 类型 | 可空 | 默认值 | 字段功能说明 |
|---:|---|---|---|---|---|
| 1 | id | bigint | 否 | nextval('"Access_id_seq"'::regclass) | 主键标识（推断：通用主键命名） |
| 2 | debug | integer | 是 | 0 | 待结合业务代码进一步确认（推断：数据库无字段注释） |
| 3 | name | varchar(50) | 否 | （无） | 名称展示字段（推断：字段名包含 name） |
| 4 | alias | varchar(50) | 是 | （无） | 待结合业务代码进一步确认（推断：数据库无字段注释） |
| 5 | get | varchar(500) | 是 | '["UNKNOWN","LOGIN","CONTACT","CIRCLE","OWNER","ADMIN"]'::character varying | 待结合业务代码进一步确认（推断：数据库无字段注释） |
| 6 | head | varchar(500) | 是 | '["UNKNOWN","LOGIN","CONTACT","CIRCLE","OWNER","ADMIN"]'::character varying | 待结合业务代码进一步确认（推断：数据库无字段注释） |
| 7 | gets | varchar(500) | 是 | '["LOGIN","CONTACT","CIRCLE","OWNER","ADMIN"]'::character varying | 待结合业务代码进一步确认（推断：数据库无字段注释） |
| 8 | heads | varchar(500) | 是 | '["LOGIN","CONTACT","CIRCLE","OWNER","ADMIN"]'::character varying | 待结合业务代码进一步确认（推断：数据库无字段注释） |
| 9 | post | varchar(500) | 是 | '["OWNER","ADMIN"]'::character varying | 待结合业务代码进一步确认（推断：数据库无字段注释） |
| 10 | put | varchar(500) | 是 | '["OWNER","ADMIN"]'::character varying | 待结合业务代码进一步确认（推断：数据库无字段注释） |
| 11 | delete | varchar(500) | 是 | '["OWNER","ADMIN"]'::character varying | 待结合业务代码进一步确认（推断：数据库无字段注释） |
| 12 | date | timestamp without time zone | 是 | now() | 待结合业务代码进一步确认（推断：数据库无字段注释） |

**关键约束与关系**

- [p] Access_pkey：PRIMARY KEY (id)
- [u] uk_access_name：UNIQUE (name)

**索引**

- Access_pkey：CREATE UNIQUE INDEX "Access_pkey" ON public."Access" USING btree (id)
- uk_access_name：CREATE UNIQUE INDEX uk_access_name ON public."Access" USING btree (name)

### Document

- 表功能：业务/配置表（推断：库中未提供表注释）
- 表注释：（无）
- 字段数：12

**字段结构与功能**

| 序号 | 字段名 | 类型 | 可空 | 默认值 | 字段功能说明 |
|---:|---|---|---|---|---|
| 1 | id | bigint | 否 | nextval('"Document_id_seq"'::regclass) | 主键标识（推断：通用主键命名） |
| 2 | debug | integer | 是 | 0 | 待结合业务代码进一步确认（推断：数据库无字段注释） |
| 3 | user_id | bigint | 否 | （无） | 关联实体主键字段（推断：字段名后缀 _id） |
| 4 | version | integer | 是 | 1 | 版本号字段（推断：版本命名） |
| 5 | name | varchar(50) | 否 | （无） | 名称展示字段（推断：字段名包含 name） |
| 6 | type | varchar(20) | 否 | （无） | 待结合业务代码进一步确认（推断：数据库无字段注释） |
| 7 | url | varchar(255) | 否 | （无） | 待结合业务代码进一步确认（推断：数据库无字段注释） |
| 8 | request | text | 否 | （无） | 待结合业务代码进一步确认（推断：数据库无字段注释） |
| 9 | apijson | text | 是 | （无） | 待结合业务代码进一步确认（推断：数据库无字段注释） |
| 10 | sql | text | 是 | （无） | 待结合业务代码进一步确认（推断：数据库无字段注释） |
| 11 | result | text | 是 | （无） | 待结合业务代码进一步确认（推断：数据库无字段注释） |
| 12 | date | timestamp without time zone | 是 | now() | 待结合业务代码进一步确认（推断：数据库无字段注释） |

**关键约束与关系**

- [p] Document_pkey：PRIMARY KEY (id)

**索引**

- Document_pkey：CREATE UNIQUE INDEX "Document_pkey" ON public."Document" USING btree (id)

### Function

- 表功能：APIJSON 远程函数配置表（来源：数据库注释）
- 表注释：APIJSON 远程函数配置表
- 字段数：10

**字段结构与功能**

| 序号 | 字段名 | 类型 | 可空 | 默认值 | 字段功能说明 |
|---:|---|---|---|---|---|
| 1 | id | bigint | 否 | nextval('"Function_id_seq"'::regclass) | 主键ID（来源：数据库注释） |
| 2 | debug | integer | 是 | 0 | 调试模式(0:关闭 1:开启)（来源：数据库注释） |
| 3 | name | varchar(50) | 否 | （无） | 函数名称（来源：数据库注释） |
| 4 | arguments | varchar(100) | 是 | （无） | 参数列表（来源：数据库注释） |
| 5 | demo | text | 否 | （无） | 函数示例（来源：数据库注释） |
| 6 | detail | varchar(1000) | 是 | （无） | 函数说明（来源：数据库注释） |
| 7 | version | integer | 是 | 0 | 版本号（来源：数据库注释） |
| 8 | tag | varchar(20) | 是 | 'Function'::character varying | 标签（来源：数据库注释） |
| 9 | methods | varchar(50) | 是 | 'GET'::character varying | 允许的请求方法（来源：数据库注释） |
| 10 | date | timestamp without time zone | 是 | now() | 创建时间（来源：数据库注释） |

**关键约束与关系**

- [p] Function_pkey：PRIMARY KEY (id)
- [u] Function_name_key：UNIQUE (name)

**索引**

- Function_name_key：CREATE UNIQUE INDEX "Function_name_key" ON public."Function" USING btree (name)
- Function_pkey：CREATE UNIQUE INDEX "Function_pkey" ON public."Function" USING btree (id)

### Request

- 表功能：业务/配置表（推断：库中未提供表注释）
- 表注释：（无）
- 字段数：8

**字段结构与功能**

| 序号 | 字段名 | 类型 | 可空 | 默认值 | 字段功能说明 |
|---:|---|---|---|---|---|
| 1 | id | bigint | 否 | nextval('"Request_id_seq"'::regclass) | 主键标识（推断：通用主键命名） |
| 2 | debug | integer | 是 | 0 | 待结合业务代码进一步确认（推断：数据库无字段注释） |
| 3 | version | integer | 是 | 1 | 版本号字段（推断：版本命名） |
| 4 | method | varchar(20) | 是 | （无） | 待结合业务代码进一步确认（推断：数据库无字段注释） |
| 5 | tag | varchar(20) | 否 | （无） | 待结合业务代码进一步确认（推断：数据库无字段注释） |
| 6 | structure | text | 否 | （无） | 待结合业务代码进一步确认（推断：数据库无字段注释） |
| 7 | detail | varchar(100) | 是 | （无） | 备注/描述说明字段（推断：语义命名） |
| 8 | date | timestamp without time zone | 是 | now() | 待结合业务代码进一步确认（推断：数据库无字段注释） |

**关键约束与关系**

- [p] Request_pkey：PRIMARY KEY (id)

**索引**

- Request_pkey：CREATE UNIQUE INDEX "Request_pkey" ON public."Request" USING btree (id)

### Script

- 表功能：APIJSON 脚本配置表（来源：数据库注释）
- 表注释：APIJSON 脚本配置表
- 字段数：8

**字段结构与功能**

| 序号 | 字段名 | 类型 | 可空 | 默认值 | 字段功能说明 |
|---:|---|---|---|---|---|
| 1 | id | bigint | 否 | nextval('"Script_id_seq"'::regclass) | 主键ID（来源：数据库注释） |
| 2 | debug | integer | 是 | 0 | 调试模式(0:关闭 1:开启)（来源：数据库注释） |
| 3 | name | varchar(50) | 否 | （无） | 脚本名称（来源：数据库注释） |
| 4 | language | varchar(50) | 否 | 'JavaScript'::character varying | 脚本语言（来源：数据库注释） |
| 5 | script | text | 否 | （无） | 脚本内容（来源：数据库注释） |
| 6 | simple | integer | 是 | 0 | 是否简化(0:否 1:是)（来源：数据库注释） |
| 7 | version | integer | 是 | 0 | 版本号（来源：数据库注释） |
| 8 | date | timestamp without time zone | 是 | now() | 创建时间（来源：数据库注释） |

**关键约束与关系**

- [p] Script_pkey：PRIMARY KEY (id)
- [u] uk_script_name：UNIQUE (name)

**索引**

- Script_pkey：CREATE UNIQUE INDEX "Script_pkey" ON public."Script" USING btree (id)
- uk_script_name：CREATE UNIQUE INDEX uk_script_name ON public."Script" USING btree (name)

### sys_department

- 表功能：科室信息表：定义医疗机构的科室逻辑结构（来源：数据库注释）
- 表注释：科室信息表：定义医疗机构的科室逻辑结构
- 字段数：8

**字段结构与功能**

| 序号 | 字段名 | 类型 | 可空 | 默认值 | 字段功能说明 |
|---:|---|---|---|---|---|
| 1 | id | bigint | 否 | nextval('sys_department_id_seq'::regclass) | 主键ID（来源：数据库注释） |
| 2 | dept_code | varchar(64) | 否 | （无） | 科室编码：国家规范或院内唯一编码（来源：数据库注释） |
| 3 | dept_name | varchar(128) | 否 | （无） | 科室名称（来源：数据库注释） |
| 4 | parent_code | varchar(64) | 是 | （无） | 上级科室编码（来源：数据库注释） |
| 5 | dept_type | varchar(32) | 是 | （无） | 科室类型：门诊/住院/医技/行政（来源：数据库注释） |
| 6 | enabled | boolean | 是 | true | 启用状态（来源：数据库注释） |
| 7 | create_time | timestamp without time zone | 是 | CURRENT_TIMESTAMP | 创建时间（来源：数据库注释） |
| 8 | update_time | timestamp without time zone | 是 | CURRENT_TIMESTAMP | 更新时间（来源：数据库注释） |

**关键约束与关系**

- [p] sys_department_pkey：PRIMARY KEY (id)
- [u] sys_department_dept_code_key：UNIQUE (dept_code)

**索引**

- sys_department_dept_code_key：CREATE UNIQUE INDEX sys_department_dept_code_key ON public.sys_department USING btree (dept_code)
- sys_department_pkey：CREATE UNIQUE INDEX sys_department_pkey ON public.sys_department USING btree (id)

### sys_login_log

- 表功能：登录日志表（来源：数据库注释）
- 表注释：登录日志表
- 字段数：6

**字段结构与功能**

| 序号 | 字段名 | 类型 | 可空 | 默认值 | 字段功能说明 |
|---:|---|---|---|---|---|
| 1 | id | bigint | 否 | nextval('sys_login_log_id_seq'::regclass) | 主键标识（推断：通用主键命名） |
| 2 | username | varchar(50) | 是 | （无） | 待结合业务代码进一步确认（推断：数据库无字段注释） |
| 3 | ip_addr | varchar(50) | 是 | （无） | 待结合业务代码进一步确认（推断：数据库无字段注释） |
| 4 | status | integer | 是 | （无） | 状态字段，用于流程或启停控制（推断：字段名包含 status） |
| 5 | msg | varchar(255) | 是 | （无） | 待结合业务代码进一步确认（推断：数据库无字段注释） |
| 6 | login_time | timestamp without time zone | 是 | now() | 待结合业务代码进一步确认（推断：数据库无字段注释） |

**关键约束与关系**

- [c] sys_login_log_status_check：CHECK (status = ANY (ARRAY[0, 1]))
- [p] sys_login_log_pkey：PRIMARY KEY (id)

**索引**

- sys_login_log_pkey：CREATE UNIQUE INDEX sys_login_log_pkey ON public.sys_login_log USING btree (id)

### sys_menu

- 表功能：系统菜单/权限表（来源：数据库注释）
- 表注释：系统菜单/权限表
- 字段数：13

**字段结构与功能**

| 序号 | 字段名 | 类型 | 可空 | 默认值 | 字段功能说明 |
|---:|---|---|---|---|---|
| 1 | id | bigint | 否 | nextval('sys_menu_id_seq'::regclass) | 主键标识（推断：通用主键命名） |
| 2 | parent_id | bigint | 是 | 0 | 父级ID（来源：数据库注释） |
| 3 | name | varchar(50) | 否 | （无） | 菜单名称（来源：数据库注释） |
| 4 | type | integer | 否 | （无） | 类型(1:目录 2:菜单 3:按钮)（来源：数据库注释） |
| 5 | path | varchar(200) | 是 | （无） | 路由路径（来源：数据库注释） |
| 6 | component | varchar(200) | 是 | （无） | 组件路径（来源：数据库注释） |
| 7 | perms | varchar(200) | 是 | （无） | 权限标识（来源：数据库注释） |
| 8 | icon | varchar(100) | 是 | （无） | 图标（来源：数据库注释） |
| 9 | sort_value | integer | 是 | 0 | 排序值（来源：数据库注释） |
| 10 | status | integer | 是 | 1 | 状态字段，用于流程或启停控制（推断：字段名包含 status） |
| 11 | create_time | timestamp without time zone | 是 | now() | 创建时间（推断：审计字段命名） |
| 12 | update_time | timestamp without time zone | 是 | now() | 更新时间（推断：审计字段命名） |
| 13 | is_deleted | integer | 是 | 0 | 布尔/标志位控制字段（推断：is_/enabled 命名） |

**关键约束与关系**

- [c] sys_menu_is_deleted_check：CHECK (is_deleted = ANY (ARRAY[0, 1]))
- [c] sys_menu_status_check：CHECK (status = ANY (ARRAY[0, 1]))
- [c] sys_menu_type_check：CHECK (type = ANY (ARRAY[1, 2, 3]))
- [p] sys_menu_pkey：PRIMARY KEY (id)

**索引**

- idx_sys_menu_parent：CREATE INDEX idx_sys_menu_parent ON public.sys_menu USING btree (parent_id)
- sys_menu_pkey：CREATE UNIQUE INDEX sys_menu_pkey ON public.sys_menu USING btree (id)

### sys_oper_log

- 表功能：操作日志表（来源：数据库注释）
- 表注释：操作日志表
- 字段数：13

**字段结构与功能**

| 序号 | 字段名 | 类型 | 可空 | 默认值 | 字段功能说明 |
|---:|---|---|---|---|---|
| 1 | id | bigint | 否 | nextval('sys_oper_log_id_seq'::regclass) | 主键标识（推断：通用主键命名） |
| 2 | title | varchar(50) | 是 | （无） | 待结合业务代码进一步确认（推断：数据库无字段注释） |
| 3 | method | varchar(200) | 是 | （无） | 待结合业务代码进一步确认（推断：数据库无字段注释） |
| 4 | request_method | varchar(10) | 是 | （无） | 待结合业务代码进一步确认（推断：数据库无字段注释） |
| 5 | operator_type | integer | 是 | （无） | 待结合业务代码进一步确认（推断：数据库无字段注释） |
| 6 | oper_name | varchar(50) | 是 | （无） | 名称展示字段（推断：字段名包含 name） |
| 7 | oper_url | varchar(500) | 是 | （无） | 待结合业务代码进一步确认（推断：数据库无字段注释） |
| 8 | oper_ip | varchar(50) | 是 | （无） | 待结合业务代码进一步确认（推断：数据库无字段注释） |
| 9 | oper_param | text | 是 | （无） | 待结合业务代码进一步确认（推断：数据库无字段注释） |
| 10 | json_result | text | 是 | （无） | 待结合业务代码进一步确认（推断：数据库无字段注释） |
| 11 | status | integer | 是 | （无） | 状态字段，用于流程或启停控制（推断：字段名包含 status） |
| 12 | error_msg | text | 是 | （无） | 待结合业务代码进一步确认（推断：数据库无字段注释） |
| 13 | oper_time | timestamp without time zone | 是 | now() | 待结合业务代码进一步确认（推断：数据库无字段注释） |

**关键约束与关系**

- [p] sys_oper_log_pkey：PRIMARY KEY (id)

**索引**

- sys_oper_log_pkey：CREATE UNIQUE INDEX sys_oper_log_pkey ON public.sys_oper_log USING btree (id)

### sys_role

- 表功能：系统角色表（来源：数据库注释）
- 表注释：系统角色表
- 字段数：8

**字段结构与功能**

| 序号 | 字段名 | 类型 | 可空 | 默认值 | 字段功能说明 |
|---:|---|---|---|---|---|
| 1 | id | bigint | 否 | nextval('sys_role_id_seq'::regclass) | 主键标识（推断：通用主键命名） |
| 2 | role_name | varchar(50) | 否 | （无） | 角色名称（来源：数据库注释） |
| 3 | role_code | varchar(50) | 否 | （无） | 角色编码（来源：数据库注释） |
| 4 | description | varchar(255) | 是 | （无） | 角色描述（来源：数据库注释） |
| 5 | status | integer | 是 | 1 | 状态字段，用于流程或启停控制（推断：字段名包含 status） |
| 6 | create_time | timestamp without time zone | 是 | now() | 创建时间（推断：审计字段命名） |
| 7 | update_time | timestamp without time zone | 是 | now() | 更新时间（推断：审计字段命名） |
| 8 | is_deleted | integer | 是 | 0 | 布尔/标志位控制字段（推断：is_/enabled 命名） |

**关键约束与关系**

- [c] sys_role_is_deleted_check：CHECK (is_deleted = ANY (ARRAY[0, 1]))
- [c] sys_role_status_check：CHECK (status = ANY (ARRAY[0, 1]))
- [p] sys_role_pkey：PRIMARY KEY (id)
- [u] sys_role_role_code_key：UNIQUE (role_code)

**索引**

- idx_sys_role_code：CREATE INDEX idx_sys_role_code ON public.sys_role USING btree (role_code)
- sys_role_pkey：CREATE UNIQUE INDEX sys_role_pkey ON public.sys_role USING btree (id)
- sys_role_role_code_key：CREATE UNIQUE INDEX sys_role_role_code_key ON public.sys_role USING btree (role_code)

### sys_role_menu

- 表功能：角色菜单关联表（来源：数据库注释）
- 表注释：角色菜单关联表
- 字段数：4

**字段结构与功能**

| 序号 | 字段名 | 类型 | 可空 | 默认值 | 字段功能说明 |
|---:|---|---|---|---|---|
| 1 | id | bigint | 否 | nextval('sys_role_menu_id_seq'::regclass) | 主键标识（推断：通用主键命名） |
| 2 | role_id | bigint | 否 | （无） | 关联实体主键字段（推断：字段名后缀 _id） |
| 3 | menu_id | bigint | 否 | （无） | 关联实体主键字段（推断：字段名后缀 _id） |
| 4 | create_time | timestamp without time zone | 是 | now() | 创建时间（推断：审计字段命名） |

**关键约束与关系**

- [p] sys_role_menu_pkey：PRIMARY KEY (id)
- [u] sys_role_menu_role_id_menu_id_key：UNIQUE (role_id, menu_id)

**索引**

- idx_sys_role_menu_menu：CREATE INDEX idx_sys_role_menu_menu ON public.sys_role_menu USING btree (menu_id)
- idx_sys_role_menu_role：CREATE INDEX idx_sys_role_menu_role ON public.sys_role_menu USING btree (role_id)
- sys_role_menu_pkey：CREATE UNIQUE INDEX sys_role_menu_pkey ON public.sys_role_menu USING btree (id)
- sys_role_menu_role_id_menu_id_key：CREATE UNIQUE INDEX sys_role_menu_role_id_menu_id_key ON public.sys_role_menu USING btree (role_id, menu_id)

### sys_user

- 表功能：系统用户表（来源：数据库注释）
- 表注释：系统用户表
- 字段数：11

**字段结构与功能**

| 序号 | 字段名 | 类型 | 可空 | 默认值 | 字段功能说明 |
|---:|---|---|---|---|---|
| 1 | id | bigint | 否 | nextval('sys_user_id_seq'::regclass) | 主键ID（来源：数据库注释） |
| 2 | username | varchar(50) | 否 | （无） | 用户名（来源：数据库注释） |
| 3 | password | varchar(100) | 否 | （无） | 密码(BCrypt加密)（来源：数据库注释） |
| 4 | name | varchar(50) | 是 | （无） | 姓名（来源：数据库注释） |
| 5 | phone | varchar(20) | 是 | （无） | 手机号（来源：数据库注释） |
| 6 | email | varchar(100) | 是 | （无） | 邮箱（来源：数据库注释） |
| 7 | avatar | varchar(255) | 是 | （无） | 头像地址（来源：数据库注释） |
| 8 | status | integer | 是 | 1 | 状态(1:正常 0:禁用)（来源：数据库注释） |
| 9 | create_time | timestamp without time zone | 是 | now() | 创建时间（推断：审计字段命名） |
| 10 | update_time | timestamp without time zone | 是 | now() | 更新时间（推断：审计字段命名） |
| 11 | is_deleted | integer | 是 | 0 | 逻辑删除(0:未删除 1:已删除)（来源：数据库注释） |

**关键约束与关系**

- [c] sys_user_is_deleted_check：CHECK (is_deleted = ANY (ARRAY[0, 1]))
- [c] sys_user_status_check：CHECK (status = ANY (ARRAY[0, 1]))
- [p] sys_user_pkey：PRIMARY KEY (id)
- [u] sys_user_username_key：UNIQUE (username)

**索引**

- idx_sys_user_deleted：CREATE INDEX idx_sys_user_deleted ON public.sys_user USING btree (is_deleted)
- idx_sys_user_username：CREATE INDEX idx_sys_user_username ON public.sys_user USING btree (username)
- sys_user_pkey：CREATE UNIQUE INDEX sys_user_pkey ON public.sys_user USING btree (id)
- sys_user_username_key：CREATE UNIQUE INDEX sys_user_username_key ON public.sys_user USING btree (username)

### sys_user_role

- 表功能：用户角色关联表（来源：数据库注释）
- 表注释：用户角色关联表
- 字段数：4

**字段结构与功能**

| 序号 | 字段名 | 类型 | 可空 | 默认值 | 字段功能说明 |
|---:|---|---|---|---|---|
| 1 | id | bigint | 否 | nextval('sys_user_role_id_seq'::regclass) | 主键标识（推断：通用主键命名） |
| 2 | user_id | bigint | 否 | （无） | 关联实体主键字段（推断：字段名后缀 _id） |
| 3 | role_id | bigint | 否 | （无） | 关联实体主键字段（推断：字段名后缀 _id） |
| 4 | create_time | timestamp without time zone | 是 | now() | 创建时间（推断：审计字段命名） |

**关键约束与关系**

- [p] sys_user_role_pkey：PRIMARY KEY (id)
- [u] sys_user_role_user_id_role_id_key：UNIQUE (user_id, role_id)

**索引**

- idx_sys_user_role_role：CREATE INDEX idx_sys_user_role_role ON public.sys_user_role USING btree (role_id)
- idx_sys_user_role_user：CREATE INDEX idx_sys_user_role_user ON public.sys_user_role USING btree (user_id)
- sys_user_role_pkey：CREATE UNIQUE INDEX sys_user_role_pkey ON public.sys_user_role USING btree (id)
- sys_user_role_user_id_role_id_key：CREATE UNIQUE INDEX sys_user_role_user_id_role_id_key ON public.sys_user_role USING btree (user_id, role_id)

## 校验摘要

- 基表数量（报告覆盖）：49
- 视图数量（报告覆盖）：6
- 触发器数量（报告覆盖）：23
- 说明：字段功能若无数据库注释，已显式标注“推断”。
